# Adex360 Migration – Progress Tracker

This file tracks progress through the phases defined in [migration-plan.md](./migration-plan.md). It is updated after every work session/prompt. For the raw, sequential "what was asked → what was done" history at the individual-task level, see [task-log.md](./task-log.md) — this file tracks status by phase/feature instead.

**Last updated:** August 27, 2026 (admin dashboard split into Blog Posts + Projects tabs, backed by new `Project`/`Industry` models — the home page's "Latest Projects" section is now fully DB-driven instead of a 4-item hardcoded array; plus a scroll-reveal fix, blog-slider loop/arrow fix, spacing/layout polish on `/portfolio` and `/resources`, and cleanup of test blog posts and unused categories/authors — all done on the separate `blog-posts` branch, see the branch-structure note below)

## Status Legend
- **Done** — phase fully complete
- **In Process** — actively being worked on right now
- Plain description — not yet started

## Current State (TL;DR)

**Twenty-eight pages are complete and shipping static:** Home (`/`), Contact Us (`/contact-us`), SEO Services (`/seo-services`), Social Media Management (`/social-media-management`), Performance Marketing (`/performance-marketing`), Web Development (`/web-development`), Shopify App Development (`/shopify-app-development`), CRM Integration (`/crm-integration`), Universal Product Feed (`/universal-product-feed`), Pushbot (`/pushbot`), Mailbot (`/mailbot`), About Us (`/about-us`), FAQ's (`/faqs`), Portfolio (`/portfolio`), BeOneShopOne (`/beoneshopone`), EU Naturals (`/eu`), AK Galleria (`/ak-galleria`), Butterfly (`/butterfly`), Logo Official (`/logo-official`), Weltew Home (`/weltew-home`), ECS (`/ecs`), Nishat USA (`/nishat-usa`), Nishat UAE (`/nishat-uae`), ONE (`/one`), Beechtree (`/beechtree`), Seona (`/seona`), Kiko Milano (`/kiko-milano`), and Terms & Privacy Policy (`/terms-and-privacy`). **All 6 originally-planned service pages, all 3 net-new individual Shopify app pages, both company pages, the Portfolio grid, all 13 of 13 individual case-study pages, and the legal page are now shipped.** All use real brand assets, real WordPress content, and the site-wide GSAP animation system — scroll-scrubbed parallax (`data-parallax`) and reversible reveals (sections animate in scrolling down and reverse out scrolling up, replaying on re-entry). GSAP is the officially recorded animation library (migration plan updated; Framer Motion dropped). Any bad/unbuilt URL shows a branded, animated "under construction" page (`src/app/not-found.tsx`) instead of the default 404 — it returns a proper 404 status; with all planned pages now shipped, it's effectively only hit by typos or dead links at this point. All placeholder Clutch widgets (hero badge + "As Featured On" strip) and the placeholder Blog/"Our Latest Media" section have been removed site-wide rather than left as fake content — they'll come back for real once the user provides a Clutch embed code and the Phase 4 blog backend exists. The repo is on GitHub — pushed and committed on both branches described below. **Note:** GitHub reports the repo has moved to `https://github.com/Adex360/adex360-nextJS.git` (previously `Munib47/adex360-nextJS.git`); pushes to the old URL still succeed via redirect, but the remote should be repointed at some point. Connect the repo to Vercel for auto-deploy on push, once a decision is made about which branch (or branches) to deploy.

**Branch structure (as of 2026-08-27):** the blog/admin/database work (Phase 4) lives on a separate `blog-posts` branch, not `main`. `main` is the live production branch serving adex360.com — it has the 28 static pages plus small standalone fixes that don't depend on the database (the `/tag/*` redirect map, a scroll-reveal bugfix). `blog-posts` has everything `main` has **plus** the full Phase 4 stack: Postgres/Prisma, NextAuth admin login, the `/admin` dashboard (Blog Posts + Projects tabs), `/resources`, and the home page's DB-driven Projects/Blog sections. The two branches diverge specifically so the live site never runs into un-provisioned-database errors; `blog-posts` will be merged into `main` (or `main` cut over to it) once the blog goes live for real. When updating this tracker, check `git log --oneline main` vs. `git log --oneline blog-posts` if unsure which branch a given change actually shipped on.

**Phase 3's page list is now fully complete — all pages built, remaining work is content depth on other phases.** The Portfolio grid (`/portfolio`) is live with real project screenshots on all 16 cards, linking to all 16 real project slugs the user provided, and all 16 now resolve to real built pages (3 Shopify app pages + 13 case studies). All 13 net-new individual case-study pages are built (`/beoneshopone`, `/eu`, `/ak-galleria`, `/butterfly`, `/logo-official`, `/weltew-home`, `/ecs`, `/nishat-usa`, `/nishat-uae`, `/one`, `/beechtree`, `/seona`, `/kiko-milano`) — the WP source reuses 3 distinct case-study shapes across these: a narrative SEO/performance/social-results style (BeOneShopOne, Butterfly, ECS, Nishat USA, Nishat UAE, ONE, Beechtree, Seona, Kiko Milano — this sub-family shares `LabeledBullets` and `ChallengeBlock`, both extracted to `src/components/casestudy/` once ECS and Nishat USA turned out to need the identical problem/solution card + labeled-bullet patterns; Kiko Milano is a clean 7th reuse of both, no new components needed), a richer "brand story" style with a stats band and cross-links (EU), and the fixed 6-section shape shared with the 3 Shopify app pages (AK Galleria, Logo Official, Weltew Home). Remaining Phase 1 asset items (service-tab illustrations, project thumbnails for the SEO/Social/Performance project grids, Clutch embed code) can be dropped in as they're exported. The SEO page's own "Free SEO Score" form (`SeoScoreCta.tsx`) still has no backend — options (Resend / SMTP / GHL webhook) were presented to the user; awaiting their choice before wiring it to send to `munib.ahmad@adex360.com`.

## Phases

### Phase 1 — Discovery & Setup
Discovery, content/asset export, final tech decisions.
**Status: In Process**
- [x] Migration plan written and documented
- [x] Next.js project scaffolded (TypeScript, Tailwind, ESLint, App Router, src/ dir)
- [x] Repo pushed to GitHub (`Munib47/adex360-nextJS`, branch `main`); Vercel connection pending (user-side, gives auto-build/deploy on every push)
- [x] Core brand assets exported and integrated: color/dark logos, white logo, hero illustration, "Who we are" illustration, 16 brand logos (`public/brands/`), 4 team photos (`public/images/team/`), favicon (`src/app/icon.jpeg`)
- [x] Home page project thumbnails (`public/images/projects/`: `butterfly.png`, `nishat-uae.png`, `logo-official.png`, `weltew-home.png`) — real screenshots, wired into `Projects.tsx` (2026-08-20)
- [ ] Remaining asset export: per-service tab illustrations (SeoStrategies tabs), project thumbnails for the SEO/Social/Performance page project grids (still placeholder gradients), blog cover images, Clutch badge/logo asset
- [x] DB choice: **local PostgreSQL 17** for development (installed 2026-08-24 via `winget`, running as a Windows service; superuser password set to `devpassword123` for local dev only). Production DB (Supabase/Neon vs. staying self-hosted) still to be decided before launch. CMS vs. custom admin panel is still undecided — see Phase 4
- [ ] Clutch widget embed code (needs `data-clutchcompany-id` from the Clutch vendor dashboard → Marketing Collateral → Widgets)

### Phase 2 — Design System Setup
Tailwind theme, shared components such as header, footer, buttons, cards.
**Status: In Process (near done)**
- [x] Tailwind theme (`src/app/globals.css`) — brand color tokens, Poppins font, hero gradient/grid, custom `xs` (480px) breakpoint. Accent orange in use across the site: `#E38A19`
- [x] Header (`src/components/layout/Header.tsx`) — fixed; transparent-over-hero with white logo, solid white + dark logo after 32px scroll; mega-menu dropdowns (orange `#E38A19` hover), Contact Us CTA. **Mobile nav rebuilt (2026-08-20)** as a premium right-to-left slide-in drawer (was a basic top-accordion panel): search bar, gradient icon-tile rows per nav item, expandable categories with an inline gradient caption card, "Start a Project" CTA + phone/email chips in the footer. Fixed a real bug where the drawer/backdrop, nested inside the `backdrop-blur` header, were being `position: fixed` relative to the header's own ~80px box instead of the viewport (CSS containing-block rule for `backdrop-filter` ancestors) — moved them to be siblings of `<header>` instead of children
- [x] Footer (`src/components/layout/Footer.tsx`) — real content (company description, 6 services, USA/Pakistan addresses/emails/phones, social links to real FB/IG/LinkedIn profiles). Responsive: 4 equal columns desktop, 2 columns tablet (480–768px), mobile (<480px) collapsible accordion rows with static logo/description
- [x] GSAP animation system (`src/components/fx/`): `ScrollFx.tsx` (data-attribute engine — staggered reveals, count-up counters, scroll-scrubbed `data-parallax` drift; reveals reverse on scroll-up and replay on re-entry; suspends CSS transitions during tweens — hard-learned fix, see Engineering Notes), `ScrollProgress.tsx` (gradient top progress bar), `BackToTop.tsx`. GSAP + ScrollTrigger is the recorded animation stack (migration plan §3 updated; no Framer Motion). Footer columns have a staggered reveal on every page
- [x] Libraries installed: `gsap` 3.15, `swiper` 14, `lucide-react` 1.31
- [ ] Extract shared UI primitives (buttons, section headers, cards) into `src/components/ui/` — still styled ad hoc per section

### Phase 3 — Static Pages Build
Home, service pages, About, FAQ, Case Studies, Contact.
**Status: Done** — all pages built; Phase 5 (SEO/JSON-LD/sitemap) and Phase 4 (blog backend) are the next open phases
- [x] **Home page — complete** (`src/app/page.tsx` + `src/components/home/*`):
  - **Hero** — real illustration, GSAP entrance timeline, 3D mouse tilt, particles/scan-line/orbs (the placeholder Clutch review badge was removed 2026-08-20, see below)
  - **Features** ("Our Magic Mantra") — 3 cards, scroll reveals, hover polish
  - **AboutStats** — `StatsShowcase.tsx`: animated 75% donut chart + 2000+ bar chart, count-up counters, floating 4.9/5 chip
  - **BrandsMarquee** — Swiper carousel of 16 real brand logos (4 visible desktop, autoplay, 1-slide steps, original colors)
  - **SeoStrategies** — interactive 6-tab services component (SEO/Web Dev/Performance/Social/CRM/Shopify) with real WP copy, per-tab client results and CTAs, GSAP tab-switch animation. Illustrations are icon-blob placeholders pending assets
  - **DrivingGrowth** ("Who We Are") — real illustration, WP copy (Orient Textiles/Hemani results), animated 350% ring card, floating stat chips, scroll parallax, line-mask heading reveal
  - **Workflow** — curved dashed path (orange→blue gradient, draws on scroll), steps pop in sync with the draw, self-drawing icons, paper plane + pulsing start dot, staggered card heights (matches WP design; a dark glass-card variant was built and reverted by request)
  - **Testimonials** — Swiper carousel with the 4 real WP testimonials (Faheem Ansari/ALMIRAH, Basit Ashrafi/SAYA, Sara Kahlo/Magna Resources, Maroof H Sabriel/MAROOF HS CPA), custom prev/next arrows, autoplay with hover-pause, 3/2/1 slides responsive
  - **Team** — real photos, corrected names/roles (Zain Hameed CEO, Umer Shoukat, Ali Lakhani, Sheharyar Ahmed), curved gradient card headers, LinkedIn hover overlay linking to real profiles, 3D flip-up entrance
  - **Projects** ("Our Latest and Completed Project") — originally 4 hardcoded cards (Butterfly, Nishat UAE, Logo Official, Weltew Home; name typo "Wellew" → "Weltew" fixed 2026-08-20) with 6 hardcoded filter tabs (All/FMCG/D2C/Apparel/Footwear/Home Decor). **Rebuilt as fully DB-driven on `blog-posts` (2026-08-25/27)** — see the Phase 4 "Projects dashboard tab" entry below for the full story; this bullet is kept for the page's build history but is no longer current for how the section actually works.
  - Site title set: "Adex360: E-Commerce Digital Marketing Agency"; favicon live via `src/app/icon.jpeg`
  - **Removed (2026-08-20):** the placeholder "Our Latest Media" blog section (`Blog.tsx`, 3 fake post cards) and the "As Featured On" Clutch strip (`ClutchStrip.tsx`) — both were fake/placeholder content the user asked to take down rather than leave live; components deleted outright (not used anywhere else). The Blog section returns for real once Phase 4's blog backend exists; the Clutch strip returns once a real Clutch embed code is provided (Phase 5)
- [x] **Social Media Management page (`/social-media-management`) — done (2026-08-19)** (`src/app/social-media-management/page.tsx` + `src/components/social/*`), built from WP screenshots + provided tab/testimonial/FAQ HTML:
  - **SocialHero** — futuristic phone-mockup composition: CSS phone with mini feed, floating 60% engagement donut / +2.4K followers / 48 mentions chips, reaction hearts that float up in a loop, rotating orbit, entrance timeline, gradient "Meta-Focused" headline (WCAG-safe stops), CTA "Boost My Socials"
  - **SocialUnique** — "Unmatched Meta and Social Media Excellence", 47% count-up, WP paragraph, stats showcase (60% donut "Increase In Per Day Sales" + dark "Record Sales In One Season" bar card), CTA "Start Your Social Success"
  - **SocialTabs** — 5-tab component (Content Creation / Community Management / Influencer Marketing / Analytics and Reporting / Social Media Strategies) with exact WP per-tab content; "Get Your Free Consultation" → LeadConnector booking widget (new tab), other CTAs → contact
  - **SocialGrowth** — "Your Social Media Growth Catalyst" (2024/63.8%/5.22B copy), animated Social Performance dashboard, chips 193% Orders (Nishat USA) / 57% Daily Sales (Noor By Sadia)
  - **SocialAdvantage** — "The Art of Doing the Bare Minimum" vs "Take Your Social Media Seriously" lists (exact WP copy)
  - **SocialProcess** — Strategize/Execute/Grow gradient-path draw; **SocialProjects** — Luxury Pret Wear / Fashion & Apparel / Heritage Fashion Ecommerce, each card a real `Link` to its own case-study slug (`/nishat-usa`, `/beechtree`, `/seona`) with a real screenshot (2026-08-21); banner → `/portfolio`
  - **SocialTestimonials** — Swiper with the 4 WP quotes (Flora B., Carlos S., Mike T., Jason G.), titles + 4.8/5; **SocialFaq** — 12 exact WP Q&As
  - Verified via tsc/eslint/build (static) + rendered-HTML content checks + a 15-agent adversarial review workflow; all 10 confirmed findings fixed (see 2026-08-19 changelog)
- [x] **Branded under-construction 404 (2026-08-19)** — `src/app/not-found.tsx` + `src/components/layout/UnderConstruction.tsx`: covers every not-yet-built route with a "This page is under construction" screen (animated 75% progress bar, morphing blob with spinning cog + tapping hammer, Work-in-progress/Launching-soon chips, Back to Home + Contact Us CTAs). Returns real 404 status; replaced automatically as each page ships.
- [x] **Performance Marketing page (`/performance-marketing`) — done (2026-08-20)** (`src/app/performance-marketing/page.tsx` + `src/components/performance/*`), built from WP screenshots + provided tab/testimonial/FAQ HTML:
  - **PerformanceHero** — "Maximize ROI, Amplify Growth", 3s hero-blob shape-cycling (site standard), 92% client-retention arc, Optimized Ad Spend / Client Retention chips, rocket badge, CTA "Power Up Your Ads"
  - **PerformanceUnique** — "Maximise ROI With Performance Marketing", static 6.81x figure (not count-up — ScrollFx's counter only animates integers, would've broken to "681x"), 60% stats showcase ("Record Sales In One Season" dark card), CTA "Maximize Your ROI"
  - **PerformanceTabs** — 5-tab component (PPC Advertising / Affiliate Marketing / Email Marketing / Display Ads / CR Optimization) with exact WP per-tab content; **all 5 CTAs → `/contact-us`** per explicit user instruction (no booking-widget link on this page)
  - **PerformanceGrowth** — "Strategize Performance Marketing Campaigns", animated Ad Performance dashboard (6.81x ROAS), Nishat USA / Noor By Sadia chips
  - **PerformanceAdvantage** — "Where Budgets Go to Die" vs "Performance That Delivers" comparison; **PerformanceProcess** — Strategize/Execute/Grow gradient-path draw
  - **PerformanceProjects** — Footwear Retail / Urban Fashion & Lifestyle Retail / Skin Care & Makeup, each card a real `Link` to its own case-study slug (`/ecs`, `/one`, `/kiko-milano`) with a real screenshot (2026-08-21); banner → `/portfolio`
  - **PerformanceTestimonials** — Swiper with the 5 WP quotes (Miles M., Warner R., Silvia P., Owen V., Brandon N.); **PerformanceFaq** — all 15 WP Q&As preserved verbatim, including the site's own duplicate-question quirk (Q5 and Q14 are both literally "What is the scope of performance marketing?" with different answers in the WP source — kept both intentionally)
  - Verified via tsc/eslint/`next build` (static) + rendered-HTML content checks (all tab labels + all always-visible copy confirmed present; the 4 non-active-tab CTAs are hydration-only, same pattern as SEO/Social pages)
  - **Content confidence flag:** 3 blocks were reconstructed from a lower-resolution full-page screenshot rather than read with full certainty — the Unique-section paragraph + its CTA wording, the Growth-section eyebrow/paragraph, and all 8 Advantage-list bullet points. Everything else (headings, all 5 tab panels, testimonials, FAQ) came from clearly legible zoomed screenshots or the user's exact HTML. Flagged to user; will swap in exact wording if they send closer screenshots of those 3 blocks.
- [x] **SEO Services page (`/seo-services`) — done (2026-08-19)** (`src/app/seo-services/page.tsx` + `src/components/seo/*`), built from WP screenshots then refined section-by-section:
  - **SeoHero** — "Elevate rankings with Adex360 SEO", GSAP entrance timeline (blob morph loop, 75% donut draw, floating Rank-#1/Organic-Growth chips, rocket badge)
  - **SeoUnique** ("Not The Only SEO Company, Just The Best!") — 70% count-up + `SeoUniqueStats` (clone of the home stats showcase: 45% orange donut, dark NEW TRAFFIC bar-chart card, 4.9/5 badge)
  - **SeoLocal** — interactive 6-tab section (Local/Technical/On-Page SEO, SEO Strategy, Keyword Analysis, SEO Audit) matching the home SeoStrategies design: per-tab WP content + CTAs, random shape-morph blob, blue active chevron. "Get Your Free Consultation" CTAs open the LeadConnector booking widget (new tab)
  - **SeoGrowth** — premium animated dashboard (self-drawing gradient growth chart, 12.5% CVR pill, 200X/6.81x stat footer, bobbing Engine/Chief Apparel chips, rotating orbit)
  - **SeoScoreCta** — dark gradient band with drifting aurora glows, masked dot grid, pulsing rocket badge, floating 92/100 score chip, split URL/email inputs + gold Check button, trust microcopy, animated success state (API pending Phase 5)
  - **SeoAdvantage** — Typical Agency vs Adex360 comparison, gradient-outlined Top Rated card, list rows cascade in
  - **SeoProcess** — 3-step gradient-path draw (Strategizing/Implementation/Refinement)
  - **SeoProjects** — 3 featured cards (D2C Western Clothing / Health & Personal Care / D2C Fashion), each a real `Link` to its own case-study slug (`/beoneshopone`, `/butterfly`, `/nishat-uae`) with a real screenshot (2026-08-21) + banner linking to **`/portfolio`** (now live)
  - **SeoTestimonials** — Swiper carousel identical to home (3/2/1 slides, gradient outlines, gutter arrows on mobile) with the 4 real WP quotes: Sara Kahlo, Maroof H Sabriel, Faulkner W., Jade P.
  - **SeoFaq** — 15-question accordion with the exact WP answers (multi-line answers preserved), staggered item reveals
  - Own metadata; mobile-first; Brands carousel reused (the Clutch strip that used to follow it was removed sitewide 2026-08-20). Pending: real illustrations (blob+icon placeholders), project thumbnails, FAQPage JSON-LD (Phase 5)
- [x] **Web Development page (`/web-development`) — done (2026-08-20)** (`src/app/web-development/page.tsx` + `src/components/webdev/*`), built from full-page + 6 zoomed tab-panel screenshots, testimonial swiper HTML, and FAQ accordion HTML:
  - **WebDevHero** — "Crafting Code, Building Futures", 3s hero-blob shape-cycling (site standard, dark-gradient hero kept for cross-page consistency rather than the WP source's literal light hero), 70% "Website Impressions Increased" arc, CTA "Let's Build It"
  - **WebDevUnique** — "Smart, Scalable & Stunning Websites", 2.5M+ stat + `WebDevUniqueStats` (50% donut "Increase In Website Sessions" + dark "200X Growth In Website Traffic" bar card), CTA "Your Vision, Our Code"
  - **WebDevTabs** — 6-tab component (Front End / Back End / Ecommerce / Site Maintenance / Performance Optimization / CMS Development) with exact WP per-tab content; all 6 CTAs → `/contact-us` (no explicit link targets were given for this page, so defaulted to the established sitewide pattern)
  - **WebDevGrowth** ("Why Our Code Wins") — "Adex360: Building Your Website Code by Code", Chief Apparel Overall-Growth/Keyword-Ranking stats; uses a teal-accented icon composition instead of a raster illustration (no image asset was provided for this section)
  - **WebDevAdvantage** — "Typical Agency" vs "Adex360" comparison; **WebDevProcess** — Design/Develop/Optimize gradient-path draw
  - **WebDevProjects** — Premium Footwear / Fashion Retail / B2C Home Décor, each card a real `Link` to its own case-study slug (`/logo-official`, `/ak-galleria`, `/weltew-home`) with a real screenshot (2026-08-21); banner → `/portfolio`
  - **WebDevTestimonials** — Swiper with the 4 unique WP quotes (deduped from the swiper's looped/duplicated slide markup: Kevin W., Taylor P., Smith O., Morgan K.); **WebDevFaq** — all 13 WP Q&As
  - Verified via tsc/eslint/`next build` (static) + rendered-HTML content checks (all 6 tab labels, every section heading, first/last FAQ question all confirmed present)
- [x] **Shopify App Development page (`/shopify-app-development`) — done (2026-08-20)** (`src/app/shopify-app-development/page.tsx` + `src/components/shopify/*`), built from full-page + 6 zoomed tab-panel screenshots, testimonial swiper HTML, and FAQ accordion HTML:
  - **ShopifyHero** — "Build Your Shopify App with Adex360", 3s hero-blob shape-cycling (site standard, blue/violet gradient blob), 70% "Increased Shopify Store Engagement" arc, CTA "Develop Your App"
  - **ShopifyUnique** — "Your Partner in Shopify App Development", 10x Increase in Sales + `ShopifyUniqueStats` (70% donut + dark "200X Traffic Growth" bar card), CTA "Elevate Your Store"
  - **ShopifyTabs** — 6-tab component (Custom App Development / UI UX Design / App Integration / Advanced Features / App Optimization / App Audit) with exact WP per-tab content; all 6 CTAs → `/contact-us`
  - **ShopifyGrowth** ("Why Our Shopify Apps Stand Out") — "Apps That Work Smarter, So You Don't Have To", Diverse-Range/Seamless-UX points; blue/violet icon composition (no raster illustration provided)
  - **ShopifyAdvantage** — "Typical Agency" vs "Adex360" comparison; **ShopifyProcess** — Design/Develop/Deliver gradient-path draw
  - **ShopifyProjects** ("Apps We Developed") — Universal Product Feed, PushBot, Mailbot, each card linking to its own real slug (`/universal-product-feed`, `/pushbot`, `/mailbot`, per explicit user instruction) rather than the usual generic gradient-card pattern; those 3 routes aren't built yet so they currently resolve to the branded under-construction page. **Real app icons added (2026-08-20):** `public/images/apps/` (`universal-product-feed.png`, `pushbot.png`, `mailbot.png`) wired in via `next/image`, replacing the lucide-icon/letter placeholders — Universal Product Feed/PushBot render `object-contain` over their colored gradient tile (icon-on-white exports), Mailbot renders `object-cover` with no extra tile since its export already has a full-bleed purple background baked in
  - **ShopifyTestimonials** — Swiper with 6 unique WP quotes deduped from the looped swiper markup (Ismail Clothing, Butterfly Pakistan, Humming Birds pk, ORIENT, FIREOX, Alpha Kleid); simplified card layout (name only, no role/avatar footer) since the source had no separate role field this time; **ShopifyFaq** — all 15 WP Q&As ("Knowledge Kit")
  - Verified via tsc/eslint/`next build` (static) + rendered-HTML content checks (all 6 tab labels, all 3 app names + slugs, first/last FAQ questions all confirmed present)
- [x] **CRM Integration page (`/crm-integration`) — done (2026-08-20)** (`src/app/crm-integration/page.tsx` + `src/components/crm/*`), built from full-page + 5 zoomed tab-panel screenshots, testimonial swiper HTML, and FAQ accordion HTML. **The last of the 6 originally-planned service pages.**
  - **CrmHero** — "Effortless CRM, Limitless Growth", 3s hero-blob shape-cycling (site standard, blue/violet gradient blob); no percentage-arc stat chip this time — the source screenshot only shows a floating "CRM/Automated" icon card, so none was invented. CTA "Power Up Your CRM"
  - **CrmUnique** — "Streamline, Automate, And Scale Effortlessly", 3M+ Automated Interactions + `CrmUniqueStats` (60% donut "Increase In Lead Conversions" + floating "150x Lead Conversions" chip), CTA "Optimize Workflows Today"
  - **CrmTabs** — **5-tab** component (Lead Nurturing / Pipeline Management / Marketing Automation / Appointment Scheduling / Reputation Management — one fewer tab than the other service pages) with exact WP per-tab content; all 5 CTAs → `/contact-us`
  - **CrmGrowth** ("Adex360: Powering Smarter CRM Solutions") — Yadea/Universal-Apps stats; orange-to-violet gradient icon composition matching the source's funnel/coins illustration
  - **CrmAdvantage** — "Typical Agency" vs "Adex360" comparison; **CrmProcess** — Assessment/Implementation/Optimization gradient-path draw
  - **No Projects/"Previous Projects" section** — confirmed absent from the source screenshots (unlike every other service page), so none was added
  - **CrmTestimonials** — same 4 WP quotes as the Web Development page (identical duplicate-heavy swiper source markup: Kevin W., Taylor P., Smith O., Morgan K.); **CrmFaq** — all 11 WP Q&As
  - Verified via tsc/eslint/`next build` (static) + rendered-HTML content checks (all 5 tab labels, every section heading, first/last FAQ questions all confirmed present)
- [x] **Universal Product Feed page (`/universal-product-feed`) — done (2026-08-20)** (`src/app/universal-product-feed/page.tsx` + `src/components/apps/*`). 1st of 3 net-new individual Shopify app pages (`docs/migration-plan.md` §4 addendum), built from a full-page screenshot + the WP article's raw HTML (an Elementor text-editor content page, not a designed marketing page like the other services — Overview/Goals/Features/Challenges/Results/Final Thoughts, no hero stats, tabs, testimonials, or FAQ in the source). Built a new reusable template, `AppCaseStudy.tsx` (dark gradient hero with an app icon + eyebrow + CTA, bulleted goals/features/results sections with check-icon list items, a "Challenges & Solutions" card list, a closing dark CTA band) driven by a typed content object (`AppCaseStudyContent`) — content lives in its own `*.content.ts` file per app so PushBot/Mailbot can reuse the same template. Icon passed as a string key (not the lucide component itself) because Server Component → Client Component props can't carry functions; `AppCaseStudy.tsx` resolves the key to a component internally. Verified via tsc/eslint/`next build` (new static route) + a rendered-HTML content smoke test.
- [x] **Pushbot page (`/pushbot`) — done (2026-08-20)** (`src/app/pushbot/page.tsx` + `src/components/apps/pushbot.content.ts`). 2nd of 3 individual Shopify app pages, built from the same `AppCaseStudy` template as Universal Product Feed with its own content file and a `Bell` icon (added to the template's icon-key map). Verified via tsc/eslint/`next build` (new static route) + a rendered-HTML content smoke test.
- [x] **Mailbot page (`/mailbot`) — done (2026-08-20)** (`src/app/mailbot/page.tsx` + `src/components/apps/mailbot.content.ts`). 3rd and last of the individual Shopify app pages, same `AppCaseStudy` template, reusing the existing `Mail` icon key with a pink-to-violet gradient to differentiate it from Universal Product Feed (blue) and Pushbot (violet-blue). Verified via tsc/eslint/`next build` (new static route, 12th total) + a rendered-HTML content smoke test. **All 3 "Apps We Developed" cards on `/shopify-app-development` now link to fully built pages.**
- [x] **About Us page (`/about-us`) — done (2026-08-20)** (`src/app/about-us/page.tsx` + `src/components/about/*`), built from full-page screenshots + WP Elementor HTML. Reuses two Home page components outright since the source content was identical: `Team` (same 4 real team members/photos) and `BrandsMarquee` (same "Brands Impacted / 2000+ Satisfied Worldwide Clients" heading, and the 4 logos this page's carousel highlighted — Kiko/Rang-Ja/Bata/Generation — already exist in `public/brands/`, part of the full existing set). New components: `AboutHero` (dark "Adex Story" card + gradient panel with 2000+/150+ count-up chips, replacing the WP source's real office photo with an icon composition since no photo asset was provided), `AboutWhatWeDo` ("Market your Brand to Latest Trends and Updates", 75% count-up stat, CTA "Grow With Us" → `/contact-us`), `AboutPerfectFit` ("Reach Every Marketing Milestone for Success", Growth Rate/Active Clients list + 90% circular progress chip), `AboutTestimonials` (same 4 WP quotes as Web Development/CRM — Kevin W./Taylor P./Smith O./Morgan K., identical source markup), `AboutProjects` ("Our Featured Projects" — 3 icon-illustration cards: D2C Western Clothing/SEO Services, Fashion Retail/Web Development, Retention Marketing/Shopify App Development — + "Adex Proud Projects!" banner → `/portfolio`). **The Clutch review-widget row at the bottom of the source page was intentionally excluded**, per explicit user instruction (consistent with the sitewide Clutch removal earlier this phase). Verified via tsc/eslint/`next build` (new static route, 13th total) + a rendered-HTML content smoke test (all section headings + team member name confirmed present; confirmed no "Clutch" string leaked in).
- [x] **FAQ's page (`/faqs`) — done (2026-08-20)** (`src/app/faqs/page.tsx` + `src/components/faqs/*`), built from full-page screenshots + the WP page's raw HTML. **The source markup had two accordions — an `elementor-hidden-desktop/tablet/mobile` legacy one (permanently hidden, contains leftover Lorem-ipsum/placeholder Q&As from the theme demo) and the real visible `n-accordion` with 10 genuine Q&As** — the hidden one was correctly identified and skipped rather than mistakenly built. `FaqHero` (simple centered "FAQ's" heading with an eyebrow, kept close to the source's minimal design). `FaqAccordion` (single-open accordion, same pattern as the service pages' FAQs) — several answers contain real internal links the WP source pointed at existing routes (`/seo-services`, `/performance-marketing`, `/social-media-management`, `/web-development`), wired as real `next/link`s instead of plain text. `FaqHelp` ("Still need help?" gradient CTA band, "Get In Touch" → `/contact-us`) — the source's filler paragraph ("These cases are perfectly simple and easy to distinguish...") was WP theme-demo Lorem-ipsum-style boilerplate, not real brand copy, so it was replaced with a genuine sentence rather than carried over verbatim. **The Clutch section was not part of this page's source and nothing was omitted.** Verified via tsc/eslint/`next build` (new static route, 14th total) + a rendered-HTML content smoke test (all 10 questions, the CTA, and all 4 internal FAQ-answer links confirmed present).
- [x] Contact Us page (`/contact-us`) — done (`src/app/contact-us/page.tsx` + `src/components/contact/ContactForm.tsx`): mobile-first. **Info column redesigned (2026-08-20)** to match a reference layout: "Talk To Us" — 2 hotline cards side by side (🇬🇧 UK `+44 7405 127633`, 🇵🇰 PK `+92 301 8220878`, the latter pulled from the footer's existing Pakistan number) — then a full-width Email card, then an "Our Offices" section (divider header) listing all **3** offices (🇬🇧 UK/Luton, 🇺🇸 US/Glendale NY, 🇵🇰 Pakistan/Lahore — previously only the UK address was shown). Flags are hand-built inline SVGs (`FlagGB`/`FlagUS`/`FlagPK` in the page file), not emoji — Windows doesn't render flag emoji (shows raw "GB"/"US"/"PK" letter fallback), so SVGs were needed for cross-platform consistency. Hotline cards stack to `sm:` (640px) instead of the cramped `xs:` (480px) breakpoint and use `break-words` instead of `truncate` so full numbers are always visible, never cut off. **Layout follow-up (2026-08-20):** UK hotline, PK hotline, and Email are now a single stacked column (dropped the `sm:grid-cols-2` on the hotline pair) and the "Our Offices" divider heading is left-aligned on desktop (leading divider line hidden at `sm:` and up) while staying centered with flanking lines on mobile. The Clutch strip below the form was removed in the same pass as the sitewide Clutch cleanup. **Form is the real GHL (GoHighLevel/LeadConnector) "Website Submission Form" iframe** (`data-form-id="wuyDI5OOWhvTnhQVrc2H"`, loaded via `api.leadconnectorhq.com/widget/form/…` + `link.msgsndr.com/js/form_embed.js` script) — submissions go directly to the user's GHL account, no custom API route needed. The card fades/scales in on scroll (GSAP, reversible) and has scroll-parallax. (Superseded the earlier React-Hook-Form static build — `react-hook-form` dependency now unused, left installed in case a future static form needs it.) **Cross-origin top-padding fix (2026-08-20):** the iframe's ~50px of internal top whitespace (`#_builder-form{margin:20px}` + `.fields-container{padding:30px 0!important}`, measured from GHL's own live stylesheet) can't be styled from our CSS — different origin, browser same-origin policy blocks it outright regardless of selector/`!important`. Visually cropped instead: `overflow-hidden` wrapper + matching `marginTop:-50px` on the iframe (self-adjusts to GHL's live auto-resize, see `GHL_TOP_CROP_PX` in `ContactForm.tsx`). The only way to apply real CSS inside the form is the form's own Custom CSS panel in the GHL dashboard.
- [x] **Portfolio page (`/portfolio`) — done (2026-08-21)** (`src/app/portfolio/page.tsx` + `src/components/portfolio/*`), built from full-page screenshots + the WP `digaluprojects` widget HTML listing all 16 real projects with their real slugs. `PortfolioHero` (minimal "Our Work / Portfolio" heading — **added for on-page SEO/UX consistency with every other interior page**; the WP source's screenshots show the grid starting directly under the nav with no heading widget at all, a deliberate deviation flagged here rather than silently invented). `PortfolioGrid` — 16 cards in a 3-column grid, each a real `Link` to its slug from the source HTML: 3 already resolve to real built pages (`/universal-product-feed`, `/mailbot`, `/pushbot`), the other 13 (`/eu`, `/beoneshopone`, `/ak-galleria`, `/butterfly`, `/logo-official`, `/weltew-home`, `/ecs`, `/nishat-usa`, `/nishat-uae`, `/one`, `/beechtree`, `/seona`, `/kiko-milano`) don't have case-study pages yet and resolve to the under-construction fallback until built (separate, not-yet-scoped follow-up). **Real images wired in (2026-08-21):** all 16 cards now show real project screenshots — fetched directly with `curl` from the exact `adex360.com/wp-content/uploads/...` URLs in the source HTML (14 unique files downloaded to `public/images/portfolio/`; `nishat.png` and `One.png` are reused twice each since the source HTML pointed 2 cards at the same file). No icon/gradient placeholders remain on this page. **The Clutch achievement-badges row at the bottom of the source was excluded per explicit user instruction**, same as every other Clutch section removed this phase. Verified via tsc/eslint/`next build` (new static route, 15th total) + a rendered-HTML content smoke test (all 16 project names/slugs confirmed present, real `next/image` srcsets rendering, zero "Clutch" leakage).
- [x] **BeOneShopOne case-study page (`/beoneshopone`) — done (2026-08-21)** (`src/app/beoneshopone/page.tsx` + `src/components/casestudy/*`). **1st of the 13 individual Portfolio case-study pages.** Built from a full-page screenshot + the WP article HTML — a results-driven SEO case study (6-month keyword-ranking/backlink/revenue growth narrative), structurally different from the app-page template (nested h2/h3/h4 sections, inline orange-highlighted stat callouts, no tabs/testimonials/FAQ). New reusable shell for all future case-study pages: `CaseStudyHero.tsx` (back-to-Portfolio link, eyebrow + title, real hero illustration fetched from the user's local XAMPP URL via `curl`), `CaseStudyCta.tsx` (closing "Want results like this?" gradient CTA band → `/contact-us` — not in the WP source, added for consistency with every other page's closing CTA), `Stat.tsx` (small bold-orange inline component for the `color:#f08821` highlighted numbers in the source). Page-specific content (`beoneshopone/BeOneShopOneArticle.tsx`) is hand-authored JSX rather than a generic data-driven renderer, since case-study content shapes vary too much page-to-page to force into one rigid schema — the hero/CTA shell is what's shared. Skipped the source's `elementor-hidden-*` "Project Info" sidebar widget (permanently hidden in the WP markup, same pattern as the FAQ page's hidden legacy accordion). Verified via tsc/eslint/`next build` (new static route, 16th total) + a rendered-HTML content smoke test (title, key stats, closing CTA all confirmed present).
- [x] **EU Naturals case-study page (`/eu`) — done (2026-08-21)** (`src/app/eu/page.tsx` + `src/components/casestudy/brand/*`). **2nd of 13, and the introduction of a second reusable case-study template** — the WP source for this page is a fundamentally different, richer "brand story" layout (dark cross-link hero referencing sibling case studies, stats band, challenge/solution/result trio, a "Services Provided" card, a "Key Teams & Expertise" band, and a "Related Case Studies" grid) rather than BeOneShopOne's narrative-article shape. New components: `BrandHero.tsx` (dark gradient hero with prev/next-style cross-links to `/beoneshopone` and `/weltew-home`, "View All Case Studies" → `/portfolio` — the WP source's literal background photo had no available URL, so a premium gradient was used in its place, consistent with how every other missing-raster-asset case this phase was handled), `BrandOverview.tsx`, `StrategyQuote.tsx`, `ChallengeSolutionResult.tsx` (real product photo + 3 icon-coded cards), `OverallResult.tsx` (38% Conversion Rate / 900% Traffic Uplift / Sub-1s Product Page Loads), `ServicesProvided.tsx`, `KeyTeams.tsx`, `RelatedCaseStudies.tsx` (hover-reveal quote cards linking to `/beoneshopone` and `/seona`, using 2 real preview images from the source). `CaseStudyCta.tsx` (built for BeOneShopOne) was made prop-configurable rather than duplicated, so this page reuses it with its own "Let's Build What's Next" copy. All 3 real images (product shot + 2 related-case-study previews) fetched from the user's local XAMPP URLs via `curl`. One content correction: the source's "Seona" related-card quote referenced an unrelated leftover brand name ("AAFMAA") from the WP theme template, not Adex360/Seona — swapped to "Adex360" to match the actual case study being linked, rather than carrying over a copy-paste artifact. Verified via tsc/eslint/`next build` (new static route, 17th total) + a rendered-HTML content smoke test (all section headings, all 3 stats, all 3 cross-links, 6 real images confirmed present).
- [x] **AK Galleria case-study page (`/ak-galleria`) — done (2026-08-21)** (`src/app/ak-galleria/page.tsx` + `src/components/casestudy/akGalleria.content.ts`). **3rd of 13 — this one's content shape is an exact match for the `AppCaseStudy` template** already built for Universal Product Feed/Pushbot/Mailbot (Overview → Development Goals & Objectives → Technologies & Features Implemented → Challenges & Solutions → Results & Impact → Final Thoughts), so it was reused directly rather than building a 3rd template — new `ShoppingBag` icon key added to `AppCaseStudy.tsx`'s icon map, teal-to-blue gradient. Added an optional `showBackToPortfolio` prop to `AppCaseStudy` (defaults to `false`, so the 3 existing Shopify app pages are unaffected) so this page — reached from Portfolio, unlike the app pages — gets a "Back to Portfolio" link; verified the other 3 app pages still render with no such link. Fixed one WP source bug while transcribing: a stray `<br>` had split "Improved Search Visibility – The SEO-friendly structure helped increase organic traffic and discoverability." across two separate bullet points ("...higher purchase rate.<br>Improved Search" / "Visibility – ...") — merged back into its intended single bullet rather than reproducing the broken split. Verified via tsc/eslint/`next build` (new static route, 18th total) + a rendered-HTML content smoke test (title, corrected bullet, Final Thoughts, and the conditional back-link on this page but absent on Universal Product Feed, all confirmed).
- [x] **Butterfly Sanitary Napkin case-study page (`/butterfly`) — done (2026-08-21)** (`src/app/butterfly/page.tsx` + `src/components/casestudy/butterfly/ButterflyArticle.tsx`). **4th of 13 — no screenshots provided this time, HTML only** (sufficient given the exact heading/copy is in the markup). Reused the BeOneShopOne narrative template (`CaseStudyHero` + `CaseStudyCta` + `Stat`) since the shape matches (image hero, h2 sections, orange-highlighted stat callouts, closing conclusion), but the source's numbered lists here use a "bold title on its own line, paragraph below" pattern (Strategies Implemented, Results Achieved, Challenges and Solutions) rather than BeOneShopOne's flat bullets — built a new shared `NumberedList.tsx` component (numbered circle badge + optional bold title + paragraph, title omitted for the plain-sentence Objectives list) instead of one-off styling this page's lists inline. Content: 20/20 keywords ranked page-one, organic traffic 10.3K → 24K (+133%) over one year. Hero illustration fetched from the user's local XAMPP URL via `curl`. Verified via tsc/eslint/`next build` (new static route, 19th total) + a rendered-HTML content smoke test (title, a numbered-list item, the 133% stat, Conclusion, and scroll-reveal marker count all confirmed).
- [x] **Logo Official case-study page (`/logo-official`) — done (2026-08-21)** (`src/app/logo-official/page.tsx` + `src/components/casestudy/logoOfficial.content.ts`). **5th of 13 — another exact match for `AppCaseStudy`**, same shape as AK Galleria (Overview → Development Goals & Objectives → Technologies & Features Implemented → Challenges & Solutions → Results & Impact → Final Thoughts). Reused the template directly with a new `Footprints` icon (violet-to-blue gradient, distinct from AK Galleria's teal-to-blue) — no template changes needed, confirming the app-page shape is genuinely reusable across unrelated brands. Verified via tsc/eslint/`next build` (new static route, 20th total) + a rendered-HTML content smoke test (title, a Challenges subsection heading, Final Thoughts, the "Back to Portfolio" link, and scroll-reveal marker count all confirmed).
- [x] **Weltew Home case-study page (`/weltew-home`) — done (2026-08-21)** (`src/app/weltew-home/page.tsx` + `src/components/casestudy/weltewHome.content.tsx`). **6th of 13 — same `AppCaseStudy` shape as AK Galleria/Logo Official**, new `Sofa` icon (brown-to-violet gradient). This page's Overview paragraph has 2 inline orange-highlighted stats ("150 stores," "80+ locations") — the first time an app-page-template overview needed that — so `AppCaseStudyContent.overview` was generalized from `string` to `ReactNode` (backward-compatible; all 5 prior pages' plain-string overviews still typecheck and render unchanged) and this page's content file uses `.tsx` with the shared `Stat` component instead of `.ts`. Verified via tsc/eslint/`next build` (new static route, 21st total, all 5 prior `AppCaseStudy` pages re-verified unaffected by the type change) + a rendered-HTML content smoke test (title, both inline stats, a Challenges subsection, Final Thoughts, and the "Back to Portfolio" link all confirmed).
- [x] **ECS case-study page (`/ecs`) — done (2026-08-21)** (`src/app/ecs/page.tsx` + `src/components/casestudy/ecs/EcsArticle.tsx`). **7th of 13**, narrative template (`CaseStudyHero` + `CaseStudyCta` + `Stat`) — this source has no "Overview" heading, just an intro paragraph straight after the H1, correctly left out rather than invented. New shared `LabeledBullets.tsx` component (bullet dot + bold label + description, label can carry an inline `Stat`) built for a "bold label: description" bullet pattern that recurs 3 separate times on this page (Key Achievements, Content Optimization/Influencer Collaborations, Steps Taken for Sales Growth) — same spirit as `NumberedList` but unnumbered, since this page's lists aren't sequential steps. The "Challenges Identified & Solutions Implemented" section's 3 problem/solution pairs were rendered as bordered cards (title + bold "Problem:"/"Solution:" labels), a local `ChallengeBlock` helper matching the visual language already established for challenge/solution content elsewhere on the site. Content: 53% sales increase, 40% faster order processing, 95% data-reporting accuracy. Hero illustration fetched from the user's local XAMPP URL via `curl`. Verified via tsc/eslint/`next build` (new static route, 22nd total) + a rendered-HTML content smoke test (title, a challenge block heading, the 53% stat, Influencer Collaborations, Conclusion, "Back to Portfolio," and scroll-reveal marker count all confirmed).
- [x] **Nishat USA case-study page (`/nishat-usa`) — done (2026-08-21)** (`src/app/nishat-usa/page.tsx` + `src/components/casestudy/nishatUsa/NishatUsaArticle.tsx`). **8th of 13**, near-identical structure to ECS (Challenges & Solutions with the same problem/solution card pattern, a "Key Achievements" stat list, 2 plain content sections, a "Sales Growth" section, Conclusion) — confirmed this shape is genuinely recurring, not a one-off, so extracted ECS's inline `ChallengeBlock` helper into a shared `src/components/casestudy/ChallengeBlock.tsx` (ECS re-verified unaffected by the refactor) rather than duplicating it a 2nd time. Content: 76% sales increase, 53% rise in website sessions, 100% growth in order count for a textile/fashion brand's social media program. Hero illustration fetched from the user's local XAMPP URL via `curl`. Verified via tsc/eslint/`next build` (new static route, 23rd total, ECS re-verified) + a rendered-HTML content smoke test (title, a challenge heading, the 76% stat, Conclusion, "Back to Portfolio," and scroll-reveal marker count all confirmed).
- [x] **Nishat UAE case-study page (`/nishat-uae`) — done (2026-08-21)** (`src/app/nishat-uae/page.tsx` + `src/components/casestudy/nishatUae/NishatUaeArticle.tsx`). **9th of 13**, narrative template — this source's "Challenges & Solutions" section is a lighter variant than ECS/Nishat USA's (title + a single "Solution:" bullet, no "Problem:" statement given), so it used a page-local `SolutionBlock` rather than the shared `ChallengeBlock` (which requires both). Heaviest stat-density page yet — 15+ inline `<Stat>` highlights across SEO rankings, domain authority, backlinks, and a Conclusion recap. Content: first-page keyword rankings up 347% (17% → 76%), domain authority +36%, backlinks +40% over 10 months. Hero illustration fetched from the user's local XAMPP URL via `curl`. Verified via tsc/eslint/`next build` (new static route, 24th total) + a rendered-HTML content smoke test (title, the 347% stat, a Challenges subsection, Conclusion, "Back to Portfolio," and scroll-reveal marker count all confirmed).
- [x] **ONE case-study page (`/one`) — done (2026-08-21)** (`src/app/one/page.tsx` + `src/components/casestudy/one/OneArticle.tsx`). **10th of 13**, narrative template, a clean 4th reuse of both `ChallengeBlock` (3 problem/solution pairs) and `LabeledBullets` (Key Achievements + 4 more labeled sections) with zero new components — confirms this shared pair now covers the majority of the narrative sub-family's content shape. Content: 55.8% increase in site traffic, 40% boost in organic sales, 30% improvement in customer retention, for a fashion retailer with 42 stores nationwide. Hero illustration fetched from the user's local XAMPP URL via `curl`. Verified via tsc/eslint/`next build` (new static route, 25th total) + a rendered-HTML content smoke test (title, a challenge heading, the 55.8% stat, Conclusion, "Back to Portfolio," and scroll-reveal marker count all confirmed).
- [x] **Beechtree case-study page (`/beechtree`) — done (2026-08-21)** (`src/app/beechtree/page.tsx` + `src/components/casestudy/beechtree/BeechtreeArticle.tsx`). **11th of 13**, narrative template, a clean 5th reuse of `ChallengeBlock` (3 problem/solution pairs) and `LabeledBullets` (Key Achievements + 4 more labeled sections) with zero new components. Content: 2.5M+ impressions, 8% of sales from recovered carts, 12% increase in conversion rates, for a high-street fashion brand's social media program. Hero illustration fetched from the user's local XAMPP URL via `curl`. Verified via tsc/eslint/`next build` (new static route, 26th total) + a rendered-HTML content smoke test (title x28, a challenge heading, "Back to Portfolio" x2, Conclusion x2, and 15 scroll-reveal markers all confirmed).
- [x] **Seona case-study page (`/seona`) — done (2026-08-21)** (`src/app/seona/page.tsx` + `src/components/casestudy/seona/SeonaArticle.tsx`). **12th of 13**, narrative template, a clean 6th reuse of `ChallengeBlock` (3 problem/solution pairs) and `LabeledBullets` (Key Achievements + 4 more labeled sections) with zero new components. Content: 30% increase in sales, 25% rise in order count, 47% improvement in conversion rate, 56% increase in average order value, for an artisanal handmade-crafts brand's social media program. Hero illustration fetched from the user's local XAMPP URL via `curl`. Verified via tsc/eslint/`next build` (new static route, 27th total) + a rendered-HTML content smoke test (title x36, a challenge heading, "Back to Portfolio" x2, Conclusion x2, the 56% stat, and 15 scroll-reveal markers all confirmed).
- [x] **Kiko Milano case-study page (`/kiko-milano`) — done (2026-08-21)** (`src/app/kiko-milano/page.tsx` + `src/components/casestudy/kikoMilano/KikoMilanoArticle.tsx`). **13th and last of 13**, narrative template, a clean 7th reuse of `ChallengeBlock` (3 problem/solution pairs) and `LabeledBullets` (Key Achievements + 4 more labeled sections) with zero new components. Content: 5X increase in ROAS, 28% reduction in cost per acquisition, 40% increase in conversion rate, for a global cosmetics brand's performance marketing program. Hero illustration fetched from the user's local XAMPP URL via `curl`. Verified via tsc/eslint/`next build` (new static route, 28th total) + a rendered-HTML content smoke test (title x28, a challenge heading, "Back to Portfolio" x2, Conclusion x2, the 5X stat, and 15 scroll-reveal markers all confirmed). **All 13 individual case-study pages are now complete — the Portfolio grid's 16 cards all resolve to real, fully-built pages.**
- [x] **Terms &amp; Privacy Policy page (`/terms-and-privacy`) — done (2026-08-21)** (`src/app/terms-and-privacy/page.tsx` + `src/components/legal/*`). The footer's "Terms & Privacy" link previously pointed at `/faqs` (a placeholder); it now links to a real dedicated page. No WP source existed for this page — content was written from scratch based on the real site (services offered, real office addresses/emails, the real GoHighLevel-powered contact/booking forms, the SEO score tool) rather than boilerplate legal filler. New `src/components/legal/` folder, following the established page schema (Hero → content → closing CTA, same as FAQ's/case studies): `LegalHero.tsx` (same minimal centered-hero pattern as `FaqHero`, "Legal" eyebrow, "Last updated" chip), `LegalNav.tsx` (sticky in-page anchor nav between "Terms of Service"/"Privacy Policy", `IntersectionObserver`-driven active-section highlight, `top-20` offset matching the header's `h-20` height), `LegalSection.tsx` (numbered-circle-badge item, same visual language as `NumberedList`/`LabeledBullets` from the case-study components), `LegalContent.tsx` (11-section Terms of Service + 12-section Privacy Policy — covers acceptance, services/engagements, IP, client responsibilities, payment, third-party platforms, liability, termination, governing law for Terms; and information collected, cookies, the real GoHighLevel third-party disclosure, data sharing/retention/security, user rights, children's privacy, international transfers for Privacy). Reused `CaseStudyCta` (already prop-configurable) for the closing CTA rather than building a new one. Verified via tsc/eslint/`next build` (new static route, 28th total) + a rendered-HTML content smoke test (both section headings, the GoHighLevel disclosure, the real email, 35 scroll-reveal markers, and the home page's footer link now pointing at `/terms-and-privacy` all confirmed).

### Phase 4 — Blog Backend & Admin
Blog backend, admin dashboard, and blog frontend pages.
**Status:** In Process, on the `blog-posts` branch (not merged to `main`) — DB, schema, staff auth, blog CRUD/frontend, and the Projects dashboard tab are all done; rich text editor and production-grade image storage/DB are still pending
- [x] Local development database: **PostgreSQL 17** installed locally (`adex360_dev`), **Prisma 7** installed and wired up (`prisma.config.ts` + `.env` → `DATABASE_URL`), connectivity verified via `prisma db pull` (2026-08-24). Prisma 7 is a very recent major version with real breaking changes from most tutorials/training data — DB URL now lives in `prisma.config.ts` (not `schema.prisma`'s datasource block), the client generator output defaults to `src/generated/prisma` instead of `node_modules/.prisma`, and **`PrismaClient` now requires an explicit driver adapter** (`@prisma/adapter-pg` + `pg`) — the old zero-arg `new PrismaClient()` no longer compiles. Checked `node_modules/prisma`'s own CLI `--help` output and generated type defs rather than assuming older Prisma conventions, same "don't trust training data" caution as the Next.js AGENTS.md note.
- [x] **Blog schema designed and migrated** (`prisma/schema.prisma`). Initial migration `20260824111333_init_blog` tied the post byline "Author" directly to the `User` login table; user feedback after trying the real form corrected this — a post's author (e.g. a team member) isn't necessarily someone with an admin login. Migration `20260824122717_split_author_from_user` split them: `User` is now login-only (staff auth, no public sign-up), and a new `Author` model (id, name) holds the byline, matching `Category`'s "pick or add new" UX. This required a full local `prisma migrate reset --force` — Prisma's own CLI detected it was being run by an AI agent and refused to proceed without explicit, unambiguous user consent per its own safety gate (a real safeguard, not a bug); the user was asked a direct yes/no confirmation before it ran. Final shape: `Category` (name + unique slug), `Author` (name), `Post` (title, unique slug, excerpt, content, featuredImage, seoTitle, seoDescription, `PostStatus` enum DRAFT/PUBLISHED, publishedAt, `tags: String[]`, authorId/categoryId relations, indexed on status+publishedAt). `prisma/seed.ts` creates the first admin `User` (from `.env`), a default "General" category, and a default Author matching the admin's name — run via `npm run db:seed`.
- [x] **NextAuth.js authentication (staff-only) — done and verified end-to-end (2026-08-24).** Installed `next-auth@4.24.15` (the stable major — v5/Auth.js is still beta after years, too risky to build on). Credentials provider (`src/lib/auth.ts`) checks email + bcrypt-compared password hash against the `User` table — no public registration, no OAuth. JWT session strategy (no NextAuth Prisma adapter needed, so no extra Account/Session/VerificationToken tables). Route handler at `src/app/api/auth/[...nextauth]/route.ts`. `/admin/login` (`src/app/admin/login/page.tsx`) is a client-side credentials form; `/admin` (`src/app/admin/page.tsx`) is a server component reading the session + a live Prisma post/category count. **Route protection hit a second Next.js 16 breaking change**: the `middleware.ts` file convention is deprecated in 16 in favor of `proxy.ts` (confirmed via the bundled `node_modules/next/dist/docs`) — `src/proxy.ts` wraps `next-auth/middleware`'s `withAuth` in an explicitly-named `proxy` function (a re-export alone wasn't recognized by Next's static check) and gates every `/admin/*` route except `/admin/login`. Verified with a real login round-trip against the live server on a throwaway port: unauthenticated `GET /admin` → 307 to `/admin/login` confirmed; POST credentials sign-in → session cookie issued; authenticated `GET /admin` → 200, rendered "Signed in as admin@adex360.com," and live Prisma-backed counts (0 posts, 1 category) confirmed.
- [x] **Admin dashboard (/admin) — Create, Edit, and Delete all done (2026-08-24).** `/admin` lists every post (title, category, Draft/Published badge, created date, Edit/Delete icons) with an "Add New Blog" button. `/admin/posts/new` and `/admin/posts/[id]/edit` share one component, `PostForm.tsx` (`src/components/admin/PostForm.tsx`), so both stay in sync: title, slug (optional — auto-generated from title via `src/lib/slugify.ts`, e.g. "Landing Pages Paid Ads and Funnels How Performance Marketing Services Work Together" → `landing-pages-paid-ads-and-funnels-how-performance-marketing-services-work-together`), excerpt (with an inline hint on what to write — a 1–2 sentence summary used on the listing page and as the SEO description fallback), content (plain `<textarea>` accepting raw HTML — rich text editor is still the planned upgrade), featured image URL, comma-separated tags, Draft/Published status, and SEO title/description. All inputs use a clearly visible `border-2 border-gray-300` (the original `border-[#E4E8F3]` was nearly invisible against the page background — direct user feedback after trying the live form). Category and Author are both `SelectWithAddNew` (`src/components/admin/SelectWithAddNew.tsx`) — a dropdown with a "+ Add New [Category/Author]" option that opens a small modal, creates the row via a server action (`createCategory`/`createAuthor`), and adds it to the dropdown immediately without a page reload. `src/app/admin/posts/actions.ts` holds `createPost`, `updatePost` (bound to a post id via `.bind(null, post.id)`), `deletePost`, `createCategory`, `createAuthor` — all re-check the session server-side. `DeletePostButton.tsx` confirms via `window.confirm` before submitting. Verified the entire Create → Edit → Delete round-trip against a live server, including reverse-engineering how Next.js encodes a *bound* server action's hidden fields (`$ACTION_REF_0` / `$ACTION_0:0` / `$ACTION_0:1`, distinct from a plain action's single `$ACTION_ID_*` field) to drive it from curl the same way a JS-disabled browser would.
- [x] **Admin dashboard All/Draft/Published filter tabs — done (2026-08-25).** `src/app/admin/page.tsx` now reads a `?status=` search param (`all` default, `draft`, `published`) and filters the post table's Prisma query accordingly, rather than always showing every post regardless of status. Three pill-style tab links sit above the table, each showing a live count (`prisma.post.count()` run 3 times in parallel — all/draft/published — independent of which tab is active, so switching tabs never shows a stale badge), with the active tab highlighted (`bg-brand-blue` vs. `text-muted`). Empty state text adapts per tab ("No posts yet..." for All vs. "No draft posts." for an empty Draft tab). Verified against the user's live dev server: confirmed the Draft tab shows only the 1 seeded test draft and hides all 6 published posts, the Published tab shows the inverse, and the All tab's badge count (7) and active-tab styling both render correctly. Deliberately verified via direct curl requests to the user's own running server rather than a competing `next build`, per the `.next`-cache-contamination lesson recorded the previous task — this change touches no Prisma schema, so no client-regeneration risk existed either way, but the caution carries forward regardless.
- [x] **Filter-tab count format changed to "(n)" — done (2026-08-25).** User asked for the count to read as `(n)` next to the label instead of a separate colored badge pill. Simplified each tab to plain text — `{tab.label} ({tabCounts[tab.key]})` — dropping the extra `<span>`/pill styling entirely rather than just re-skinning it, since the request was for the simpler inline format. Verified against the user's live dev server: tabs now render "All (7)", "Draft (1)", "Published (6)".
- [x] **`/resources` conditional real-listing-vs-under-construction logic — done (2026-08-24).** `src/app/resources/page.tsx` counts `PUBLISHED` posts: zero renders the existing branded `UnderConstruction` component (same as any other not-yet-built page); one or more renders a real card-grid listing (title, excerpt, category, author, date) with each card linking to `src/app/resources/[slug]/page.tsx`, a full post-detail page rendering the stored HTML content. Both routes are `force-dynamic` for now (no caching to fight while there's no content pipeline yet — ISR is a deliberate follow-up once real posts exist and traffic matters). Verified all 4 states live: 0 posts → under-construction; publish one → real listing + working detail page; delete it → reverts to under-construction automatically. **This listing/detail UI is intentionally basic** — the user has real WordPress-era blog page layouts (desktop + mobile) to share next, which will replace this with the real design; this pass only had to prove the on/off content-gating behavior works.
- [x] **Featured image upload — done (2026-08-24), local-disk only.** Replaced the plain URL text field with a real file picker: `src/components/admin/ImagePicker.tsx` (client component — file input styled as a button, live thumbnail preview via `URL.createObjectURL`, and a hidden `existingFeaturedImage` field carrying the current image forward on edit when no new file is chosen). Server side, `saveFeaturedImage()` in `src/app/admin/posts/actions.ts` reads the uploaded `File` straight off the `FormData` (Next.js Server Actions accept files natively — no separate upload endpoint needed), writes it to `public/images/blog/<timestamp>-<slugified-original-name><ext>` via `fs/promises`, and returns its public `/images/blog/...` path for the `Post.featuredImage` column. Verified with a real file upload via curl (had to switch from Git-Bash-style `/c/...` paths to `C:/...` — this environment's `curl` is a native Windows build via mingw64, not MSYS, so it doesn't translate POSIX-style paths for `-F file=@path`): confirmed the file landed on disk with the correct name, the DB column stored the right path, and — importantly — that the image only 404'd against a `next start` (production) test server because production mode snapshots the static file list at boot; re-verified against the user's actual `npm run dev` process (which serves `public/` live from disk) and got a clean 200, so this works correctly in the workflow the user actually uses. **This is local-disk storage only** — it won't survive a Vercel deployment (serverless functions have an ephemeral/read-only filesystem), so the already-tracked "Image upload (Cloudinary or Vercel Blob)" Phase 5 item is still real future work before this ships to production; flagged here so it isn't forgotten once local dev has real content in it.
- [x] **Featured image picker UX polish — done (2026-08-25).** User feedback on the first pass: clicking the thumbnail should open the file dialog (not a separate native "Choose File" button), and there needed to be a way to clear a selected image. Reworked `ImagePicker.tsx`: the native `<input type="file">` is now visually hidden entirely, and both the thumbnail box and a "Change image"/"Choose image" text link are wrapped in `<label htmlFor>` elements pointing at it (multiple labels for one input is valid HTML — both trigger the native file dialog with zero JS). Added a "Remove image" action (only shown once an image exists) that clears the preview, resets the hidden file input via a `ref`, and swaps the hidden `existingFeaturedImage` field for a `removeFeaturedImage=true` one — `saveFeaturedImage()` in `actions.ts` checks that flag first and returns `null` regardless of any existing image, so removal is an explicit action distinct from "no new file chosen, keep the old one." Verified against the user's own live `npm run dev` server (not a throwaway test instance): confirmed the new hidden-input/Remove-image/Change-image markup renders, and drove the actual remove round-trip through the real edit-post server action, confirming `featuredImage` went to `NULL` in Postgres. **Investigated the broken-image icon from the user's screenshot as part of this pass**: the file (`/images/blog/1787641594190-new-69.jpeg`) was already valid on disk and serving HTTP 200 by the time this was checked — likely a stale browser cache or a screenshot taken mid-upload, not an actual bug; no fix was needed. The real post's image was temporarily set to `NULL` during the remove-flow verification and restored to its original value immediately after, since it's the user's real content, not test data.
- [x] **Orphaned image cleanup on disk — done (2026-08-25).** Removing or replacing a featured image previously only updated the database column — the old file stayed in `public/images/blog/` forever, and deleting a post left its image behind too. Added `deleteLocalImage()` to `actions.ts` (only ever touches paths under `/images/blog/`, silently no-ops on external URLs or already-missing files) and wired it into all 3 places a file can become orphaned: `saveFeaturedImage()` deletes the old file when a new one is uploaded in its place, and deletes it outright (no replacement) when `removeFeaturedImage=true`; `deletePost` now looks up the post's `featuredImage` before deleting the row and removes that file too. Verified all 3 paths for real against the user's live dev server with disposable test images (never touching the user's real post's image this time): created a post with an uploaded image → confirmed the file existed on disk → replaced it with a different image → confirmed the *old* file was gone and the new one present → deleted the post entirely → confirmed its image file was gone too, while the real, unrelated post's image was untouched throughout.
- [ ] Rich text editor integration (TipTap or Lexical) — content field is a plain HTML textarea for now
- [ ] Production-ready image storage (Cloudinary or Vercel Blob) — current uploads only work for local dev, not a serverless deploy
- [ ] Production database choice (Supabase/Neon Postgres vs. MongoDB Atlas vs. staying self-hosted) — separate from the local dev DB above, still undecided
- [x] **Real `/resources` + `/resources/[slug]` design — done (2026-08-25).** User shared their old WordPress blog listing layout (desktop + mobile screenshots) and asked for it rebuilt on the new stack rather than copied verbatim — kept the structural/UX elements (2-column card grid, date/author/view-count meta row with icons, a blue "Read More" pill button, numbered pagination) but swapped the WP-era generic illustration art for the real `featuredImage` uploads the admin panel already produces. Rebuilt `src/app/resources/page.tsx`: grid narrowed from 3 columns to 2 (`md:grid-cols-2`) to match the reference; new `src/components/resources/Pagination.tsx` (10 posts per page via `?page=N` search param, smart ellipsis for many pages, prev/next arrows, `aria-current` on the active page) — genuinely paginates via Prisma `skip`/`take`, not a fake "load more" button. **Added a real `views` counter** (`Post.views Int @default(0)`, new migration `20260825093238_add_post_views`) rather than fabricate the view-count numbers visible in the WP reference — `/resources/[slug]/page.tsx` increments it by 1 on every real page load via `prisma.post.update({ data: { views: { increment: 1 } } })`, separate from the `generateMetadata` call so a single visit doesn't double-count. Both listing cards and the detail page now show Calendar/User/Eye icon rows for date/author/views. Verified thoroughly against a fresh, isolated test server (deliberately not the user's own long-running `npm run dev` process, to avoid contaminating their session) with 12 temporary posts: page 1 correctly showed 10 cards, page 2 the remaining 2, in the right date order; view counts incremented by exactly 1 per real visit across repeated requests. All pagination test posts were deleted afterward; the user's own 6 `[TEST]` posts were left untouched.
  - **Known gotcha surfaced and explained to the user**: running `npx next build` (production) for verification while the user's own `npm run dev` is live can leave their dev server's `.next` cache showing a stale Prisma client (the views count rendered as blank rather than `0` on their live server even after it happened to restart) — both dev and prod builds default to writing into the same `.next` directory, so a build run mid-session can cross-contaminate the dev server's cache. Advised the standard fix (stop `npm run dev`, delete `.next`, restart) and flagged as something to be more careful about in future verification passes — see the note in the Engineering Notes section.
- [x] **`ReadMoreButton` skewed hover-sweep effect — done (2026-08-25).** User provided a detailed component spec (pill button, `::after` overlay with `translateY(-45%) skew(25deg) scale(0→1.2)` on hover/focus, CSS-variable-driven colors) written in generic "CSS Modules or styled-components" terms — checked the codebase first per the spec's own instruction and found neither exists anywhere in this project (pure Tailwind throughout), so implemented the identical visual effect with Tailwind's `after:` pseudo-element variant and arbitrary-value utilities instead of introducing a new styling system for one component. New `src/components/resources/ReadMoreButton.tsx`: renders as a real `next/link` `<Link>`, colors configurable via `--btn-bg`/`--btn-hover-bg`/`--btn-text` CSS custom properties set through the `style` prop (defaults `#0c5adb`/`#03112d`/`#ffffff`), the overlay uses `after:z-[-1]` so it sweeps above the button's own background but stays behind the in-flow "Read More" text (this ordering is guaranteed by the CSS stacking-context spec, not incidental — negative-z-index children paint above their parent's background but below normal in-flow content). Hover (`hover:after:scale-[1.2]`) and keyboard focus (`focus-visible:after:scale-[1.2]`) both trigger the identical sweep. **Applying it required restructuring the `/resources` cards**: they were previously one whole-card `<Link>`, which can't contain a second real `<Link>` (invalid nested anchors) — changed each card to a plain `<div>` wrapper with an inner `<Link>` around the image/meta/title (preserves click-anywhere-on-that-area UX) and `ReadMoreButton` as a sibling `<Link>` at the bottom, both pointing to the same post. Verified against the user's live dev server (skipped a competing `next build` per the `.next`-cache lesson above): confirmed the exact class list renders in the HTML and — since Tailwind's arbitrary-value classes only exist if genuinely compiled — confirmed the actual generated stylesheet contains the `25deg` skew rule and both `var(--btn-bg)`/`var(--btn-hover-bg)` references, not just checking the source code compiles.
- [ ] ISR for the blog listing/detail pages — currently `force-dynamic`
- [x] **Wire Home page Blog section to real posts — done (2026-08-25).** `src/app/page.tsx` (now an async Server Component) fetches the 6 most recently published posts via Prisma and only renders the new `src/components/home/BlogPosts.tsx` section when at least one exists — condition mirrors `/resources`'s own construction-page gate, so the two stay consistent. `BlogPosts.tsx` is a Swiper carousel (same visual language/pattern as the home `Testimonials.tsx` slider — arrow buttons, `data-reveal` entrance): 1 slide on mobile, 2 on tablet, 3 on desktop, with `slidesPerGroup` matching `slidesPerView` at each breakpoint so "Next" pages straight from posts 1&ndash;3 to 4&ndash;6 rather than smooth-scrolling one at a time, matching the user's "3 visible, rest shown on click" spec exactly. Cards reuse the sitewide hover-arrow treatment (`rotate-45` on hover, the standard established across the Portfolio grid and service project cards) and link to `/resources/[slug]`. Prev/next arrows and the "View All Posts" link only render once there are more than 3 posts (no pointless controls for 1&ndash;3 posts). **This makes `/` a dynamic route** (`export const dynamic = "force-dynamic"`) instead of static, since it now reads live, frequently-changing DB state — the same trade-off already accepted for `/resources`; both are flagged together as later ISR candidates rather than staying `force-dynamic` forever. Verified live against the user's real dev server end-to-end: with 1 real published post, the section renders with exactly that 1 card and no arrows; created 5 disposable test posts to reach 6 total and confirmed all 6 titles rendered with both prev/next arrow sets now present; deleted the 5 test posts and confirmed the section correctly dropped back to showing just the 1 real post, with zero leftover test data.
- [x] **BlogPosts slider UX revision — done (2026-08-25), per user feedback on the first pass.** Two changes: (1) the header-row arrow pair was removed entirely — arrows now live only beside the slider itself (`absolute left-0`/`right-0`, vertically centered), visible at every breakpoint instead of being desktop-only-in-header + mobile-only-beside-slider as before; slider side padding widened (`px-11` → `lg:px-14`) so the larger desktop arrows (`lg:h-11 lg:w-11`) never overlap the cards. (2) removed the `slidesPerGroup` overrides that matched `slidesPerView` per breakpoint — Next/Prev now always steps one slide at a time regardless of how many are visible, instead of paging a full screen's worth per click. `slidesPerView` (1/2/3 responsive) is unchanged. Verified the new arrow markup renders at all breakpoints (not just mobile) against the user's live dev server.
- [x] ~~BlogPosts slider loop enabled (2026-08-25) via `loop={posts.length > 1}`~~ — **superseded 2026-08-27**, see the `watchOverflow`/`isLocked` entry below; the flat `posts.length > 1` check let the arrows and loop appear even when a breakpoint had nowhere to slide to (e.g. 2 posts on a 3-per-view desktop layout).
- [x] ~~6 test blog posts seeded for the client's manual QA pass (2026-08-25)~~ — **deleted 2026-08-27**, see "Test posts and unused taxonomy cleaned up" below. All 6 `[TEST]`/`[TEST DRAFT]` posts removed once QA was done.
- [x] **Projects dashboard tab — done (2026-08-25/27), on `blog-posts`.** The home page's "Latest Projects" section (previously 4 hardcoded cards, 6 hardcoded filter tabs — see the Phase 3 Home-page note above) is now a second admin-manageable content type alongside blog posts. New Prisma models: `Project` (name, service, image, status, an `industryId` relation — no case-study link, no manual slug/summary/"featured" fields; all removed after user feedback that cards are display-only, not navigation) and `Industry` (a lookup table that doubles as the home page's filter tabs — a project in a brand-new industry adds that tab automatically, and a tab only ever shows if a project actually belongs to it). `service` is deliberately **not** a relation — it's a fixed 6-item list derived from the site's own nav (`src/lib/services.ts`), so it can never drift out of sync with the real service pages. `/admin` (`src/app/admin/page.tsx`) is now a thin router over `?tab=posts|projects` + `?status=`, delegating to `PostsPanel`/`ProjectsPanel` (`src/components/admin/*`) — both show 4 stat cards and the same All/Draft/Published pills (generalized into a shared `StatusFilterTabs`). **The home page section is fully automatic, no manual toggle**: it always shows the `HOME_PROJECTS_COUNT` (4) most recently published projects (`src/lib/projects.ts`), and the admin table computes an "On Home" badge by checking membership in that same query rather than storing a flag — the home page query and the dashboard indicator can't drift out of sync because they're driven by the same source of truth. Blog and project image uploads share one `createImageStore(urlPrefix)` helper (`src/lib/imageStore.ts`) instead of duplicated fs code; project uploads land in `public/images/projects/uploads/`, separate from the committed screenshots, so deleting a seeded project can never unlink a file that's in git. Seeded with all 16 real projects from `/portfolio`'s `PortfolioGrid.tsx` (not just the original 4) — industry assigned by judgment per project since the portfolio page itself only tracks service, not vertical (e.g. "Premium Footwear" → Footwear industry, corrected from an earlier "D2C" guess; the 3 Shopify-app products got a new "Shopify Apps" industry). Verified across 3 migrations (add Project/Industry → drop caseStudyUrl/sortOrder → drop summary/featured) with `tsc`/`eslint`/`next build` plus logged-in smoke tests against a live server at every step.
- [x] **Scroll-reveal fix for filter-driven layout changes — done (2026-08-25).** Clicking a Projects filter tab that showed fewer results shrank the page, but every `ScrollFx` `ScrollTrigger` below it was still measured against positions cached at mount — their start points ended up past the new (shorter) end of the document, so Team and the footer got stuck at `opacity: 0` forever. Fixed generically in `src/components/fx/ScrollFx.tsx` (not in `Projects.tsx`) with a `ResizeObserver` on `document.body` that debounced-calls `ScrollTrigger.refresh()` on any content-driven height change — covers this bug class for any future filter/tab/accordion UI, not just Projects.
- [x] **Blog slider: arrows/loop only active when a breakpoint actually overflows — done (2026-08-27).** The home blog slider's breakpoints are 1/2/3 slides per view (mobile/tablet/desktop); previously the nav arrows appeared whenever there were 2+ posts total, even on a breakpoint with nothing to slide to (e.g. 2 posts on the 3-per-view desktop layout — a real bug the user caught by screenshot). Uses Swiper's `watchOverflow` option, which locks the slider whenever the post count doesn't exceed the current breakpoint's `slidesPerView`; that lock state is mirrored into React (since the custom arrow buttons aren't Swiper's own nav component, its automatic button-hiding doesn't reach them) and also gates `loop` (on only once there's something to actually loop through — Swiper's docs note `loop` and `watchOverflow` conflict otherwise). Net effect matches what was asked: slider activates at 2+ posts on mobile, 3+ on tablet, 4+ on desktop.
- [x] **Layout polish: spacing + 3-column blog grid — done (2026-08-27).** Added top padding to the grid sections on `/portfolio` and `/resources` (both previously had zero space between their hero-style heading and the grid below — visually touching); widened the home blog slider's own header-to-slider gap. `/resources`' post grid changed from 2 columns to 3 on desktop (`max-w-4xl md:grid-cols-2` → `max-w-6xl sm:grid-cols-2 lg:grid-cols-3`).
- [x] **Test posts and unused taxonomy cleaned up — done (2026-08-27).** Deleted all 6 `[TEST]`/`[TEST DRAFT]` posts seeded 2026-08-25 for QA (identified by their deterministic `testpost...` seed ids, leaving the one genuine post — "Landing Pages Paid Ads and Funnels…" — untouched at the time). Also removed the unused "Adex360" blog category (0 posts) and "Munib Ahmad" author (0 posts), and renamed the default "Adex360 Admin" author to "Adex360" (`.env`'s `ADMIN_NAME` updated to match, so a future `npm run db:seed` run stays consistent instead of recreating the old name). **Open item surfaced during this cleanup:** the one real post that existed right after the test-post deletion was gone by the time the taxonomy cleanup ran a few messages later — nothing in this session touched the `Post` table in between, so it was most likely deleted through the dashboard UI directly (by the user or someone with the login) rather than by anything done here. Flagged to the user, unresolved.
- [x] **All of the above pushed to `blog-posts`** (`4b28b93..6bf522e`) — commit `6bf522e`, 29 files changed. `main` was not touched by any of this; it only ever received the `/tag/*` redirect map and the `ScrollFx` resize-observer fix (see Phase 6/Phase 2 respectively), both cherry-picked or file-extracted across on purpose, never a full merge.

### Phase 5 — SEO & Contact Form Integration
Metadata, schema, sitemap, and contact form integration.
**Status:** Not started
- [ ] Next.js Metadata API on every page (root title/description done; per-page pending)
- [ ] JSON-LD schema: Organization/LocalBusiness, Service, BreadcrumbList, FAQPage, Article/BlogPosting, Review
- [ ] sitemap.xml and robots.txt
- [x] ~~Contact form API route + email delivery~~ — not needed; Contact Us now uses the GHL iframe embed directly (2026-08-20)
- [ ] SEO-score form (`SeoScoreCta.tsx`) still needs API route + email delivery to `munib.ahmad@adex360.com`. Options presented to the user 2026-08-20: **Resend** (recommended — simple API, generous free tier, no dependency on the user's mailbox), **SMTP** via the existing `@adex360.com` mailbox (nodemailer, no new third-party service but more fragile at scale), or a **GoHighLevel webhook** (fits their existing GHL-centric stack, also creates a lead record, but needs a GHL workflow set up on their end first). Awaiting their choice before implementing
- [ ] Spam protection (honeypot or reCAPTCHA v3)
- [ ] next/image optimization pass, alt text audit
- [ ] Official Clutch review widget embed (replaces custom hero badge + ClutchStrip once embed code is provided)

### Phase 6 — QA & Redirects
QA, performance tuning, and redirect mapping.
**Status:** In Process — site is already live (see Phase 7); redirect map started reactively based on real 404s Google is surfacing, not run proactively as a full audit yet
- [x] **`/tag/:slug` → real page redirects — done (2026-08-25), reactive fix, not the full redirect map.** User discovered via a live Google search for "adex360" that old WordPress tag-archive URLs (e.g. `/tag/best-seo-company-in-the-world`) are still indexed and getting real search traffic, but 404 on the new site. Rather than a fixed list of exact old→new URL pairs, used Next.js `redirects()` in `next.config.ts` with path-to-regexp custom per-segment regex (`/tag/:slug(.*seo.*)` etc.) to keyword-match the old slug and 301/308-redirect to the closest real page — `seo`→`/seo-services`, `social`→`/social-media-management`, `performance`→`/performance-marketing`, `shopify`→`/shopify-app-development`, `crm`→`/crm-integration`, `web-dev`/`website`→`/web-development`, with a catch-all `/tag/:path*` → `/resources` for anything else (closest real equivalent to a WP tag archive, which listed blog posts). **This only covers `/tag/*` URLs surfaced so far — it is not the full "old WordPress URLs → new Next.js URLs" redirect map** that Phase 6 originally scoped; other old WP URL patterns (individual old post slugs, category archives, author pages, etc.) haven't been audited yet and may need their own redirects once/if they surface the same way.
- [ ] Full URL redirect map (old WordPress URLs → new Next.js URLs) + 301s — beyond the reactive `/tag/*` fix above, a proper audit (Google Search Console's Coverage report, or crawling the old site's URL list) hasn't been done
- [ ] Cross-browser and mobile responsiveness testing
- [ ] Form submission testing
- [ ] Broken-link checks
- [ ] Schema validation (Google Rich Results Test)
- [ ] Lighthouse performance/SEO audit (compress team PNGs ~300-900KB each while at it)

### Phase 7 — Launch & Monitoring
Launch and post-launch monitoring.
**Status:** In Process — **the site is live on adex360.com** (discovered 2026-08-25 via the user's own Google search, not a planned/announced cutover in this log — noting the fact so this tracker matches reality, not the original phase order)
- [x] DNS cutover — done, timing/date not communicated to this log, discovered retroactively
- [ ] Submit updated sitemap to Google Search Console — no `sitemap.xml` exists yet (Phase 5 item), and unclear if GSC has been set up for the new site at all
- [ ] Monitor GSC and analytics for 2–4 weeks post-launch — can't start until the above is set up
- **Practical consequence of launching before Phase 6 QA/Phase 5 SEO wiring finished:** old WordPress URLs Google indexed (tag archives confirmed so far) 404 on the live site until redirected; per-page JSON-LD, sitemap.xml/robots.txt, and the SEO-score form's email delivery are all still outstanding Phase 5 items now live on a real, publicly-indexed domain rather than a pre-launch staging site.

## What's Next (recommended order)

**Phase 3 (all static pages) and the core of Phase 4 (blog + Projects dashboard) are both done.** The DB/CMS decision that used to block Phase 4 was resolved (local PostgreSQL + Prisma + a custom admin, not a 3rd-party CMS). Remaining open items, in rough priority order:

1. **Decide when `blog-posts` merges into (or replaces) `main`.** Right now the live production site (`main`) has no blog/admin at all; the full Phase 4 stack only exists on `blog-posts`. Until this is decided, every future blog/dashboard change needs to keep landing on `blog-posts`, and any `main`-only fix (like the `/tag/*` redirects) needs to be carried across by hand so the branches don't drift further apart.
2. **Track down the missing real blog post** ("Landing Pages Paid Ads and Funnels…") — flagged 2026-08-27, cause unconfirmed. Low urgency (no real blog content exists yet beyond it) but worth a quick check before more posts get written.
3. **Production-readiness gaps on `blog-posts`** before it could ever go live: image uploads are local-disk only (won't survive a serverless deploy — Cloudinary/Vercel Blob still needed), and the production database choice (stay self-hosted Postgres vs. Supabase/Neon) is still undecided, separate from the local dev DB.
4. **Phase 5 items with no open decision — can start any time, on either branch:** per-page Next.js Metadata API audit (root is done, per-page pending), JSON-LD schema (Organization/LocalBusiness sitewide, Service on each service page, BreadcrumbList, FAQPage on `/faqs`), `sitemap.xml`/`robots.txt`, `next/image` optimization + alt-text audit. These matter more now that the site is publicly live and indexed (see Phase 7).
5. **SEO-score form email delivery** — waiting on the user to pick Resend / SMTP / GHL webhook (see Phase 5); quick to wire up once decided.
6. **Full old-WordPress-URL redirect audit** (Phase 6) — the `/tag/*` fix only covers what's surfaced reactively via Google search so far, not a proactive crawl of the old site's full URL list.
7. **Rich text editor** for the blog content field (currently a raw HTML textarea) — lower priority than the above since the admin UI is fully functional without it.
8. **Confirm 3 flagged Performance Marketing content blocks** (Unique paragraph/CTA, Growth eyebrow/paragraph, Advantage's 8 bullets) — sent to user for closer screenshots; low-priority polish, page already ships.
9. **Remaining assets** — service-tab illustrations, Clutch embed code; drop into `public/images/` and swap in (the Clutch strip/badge stays removed until a real embed code exists). Portfolio's project thumbnails are done.

~~Individual case-study pages~~ — **done (2026-08-21).** All 13 shipped; the Portfolio grid's 16 cards all resolve to real, fully-built pages.
~~Commit + push the site's static-page work~~ — **done.** All work is committed and pushed on both `main` and `blog-posts` as of 2026-08-27; see "Branch structure" above.

## Engineering Notes (read before touching animations)
- **Never put CSS `transition-all` (or any transition covering `opacity`/`transform`) on an element GSAP animates** — the transition fights GSAP's per-frame writes and elements get stuck invisible. `ScrollFx.tsx` suspends transitions during tweens and restores them via `clearProps`; custom-animated sections (Workflow, Team, DrivingGrowth) follow the same suspend/clearProps pattern manually.
- `ScrollFx` API: `data-reveal="up|left|right|scale|fade"`, `data-reveal-group` (+ `data-stagger`), `data-reveal-delay`, `data-counter` + `data-counter-suffix`, `data-parallax="N"` (scroll-scrubbed ±N% yPercent drift; avoid on elements with CSS hover transforms). Sections with bespoke GSAP timelines (Hero, DrivingGrowth, Workflow, Team, SeoStrategies tab-switches, StatsShowcase charts, SeoHero, SeoGrowth, SeoScoreCta) manage their own animation and mostly bypass `data-reveal`.
- **Hero blob shape cycling (standard for all service pages):** hero gradient blobs auto-morph to a fresh random CSS shape family (blob/arch/diamond/parallelogram/egg/leaf/squircle) every 3s — shared makers live in `src/lib/blobShapes.ts` (`pickShape`), pattern reference `SeoHero.tsx` (recursive `gsap.delayedCall(3, …)`, elastic ease, icon counter-transform). The tab components use the same lib for on-click morphs.
- **Reveals are reversible:** ScrollFx reveals and the bespoke section timelines use play-on-enter / reverse-on-leave-back (`toggleActions: "play none none reverse"` or explicit `onLeaveBack`), so sections replay when scrolled back into view. CSS transitions are re-suspended before reversing. Counters intentionally stay one-shot; parallax is inherently bidirectional.
- All animation respects `prefers-reduced-motion`; SSR markup always renders the finished state for SEO.
- Custom breakpoints: `xs` = 480px (defined in `globals.css` `@theme`); Tailwind defaults otherwise (md 768px = tablet/desktop boundary per client).
- Dev: `npm run dev` (hot reload). Prod check: `npm run build` + `npm run start` — never both on port 3000 at once.
- **`npx next build` and `npm run dev` share the same `.next` output directory by default.** Running a verification build while the user's own dev server is live can leave that dev server serving a stale module (seen 2026-08-25: a freshly-added Prisma `views` field rendered blank instead of `0` on the user's long-running `npm run dev`, even after it restarted, until they deleted `.next` and started clean). No code-level fix for this — just be aware it can happen, and if a live-server check shows something inexplicably missing right after a build+verify pass, suspect this before assuming a real bug. Fix is always: stop `npm run dev`, delete `.next`, restart.

## Change Log
- **2026-08-18:** Project scaffolded as "Adex360 - NextJS"; migration-plan.md and progress.md created inside /docs.
- **2026-08-18:** Built Home page from Elementor design screenshots — brand Tailwind theme, sticky Header (mega-menu + mobile), Footer, and all Home sections. Verified with `tsc --noEmit`, `eslint`, and `next build` (static, no errors).
- **2026-08-18:** Real logos added; Header rebuilt (transparent-over-hero → solid on scroll, white/dark logo swap); repo pushed to GitHub (`Munib47/adex360-nextJS`). Brands section rebuilt as Swiper carousel with 16 real brand logos.
- **2026-08-18:** Hero got real illustration + Clutch review badge; GSAP installed; futuristic Hero animation (entrance timeline, 3D mouse tilt, particles, scan-line, animated grid).
- **2026-08-18:** Site-wide GSAP animation system — `ScrollFx.tsx` data-attribute ScrollTrigger engine (staggered reveals + count-up counters across all sections), scroll progress bar, back-to-top button. Fixed the CSS-transition-vs-GSAP conflict that left sections invisible (see Engineering Notes).
- **2026-08-18:** SeoStrategies rebuilt as interactive 6-tab services component with real WP copy/results and GSAP tab-switch animations. AboutStats rebuilt as `StatsShowcase` (animated donut + bar charts).
- **2026-08-19:** DrivingGrowth rebuilt with real illustration, WP copy, animated 350% ring, floating chips, parallax. Workflow rebuilt with gradient curved-path draw animation, synced step pops, self-drawing icons, paper plane (dark variant built then reverted by request). Testimonials rebuilt as Swiper carousel with the 4 real WP testimonials. Team rebuilt with real photos, corrected names, LinkedIn hover overlays, 3D entrance.
- **2026-08-19:** Footer: real content (description, services, USA/Pakistan contacts, social URLs) + responsive rework (4 equal cols desktop / 2 cols tablet / mobile accordion, custom 480px breakpoint). Favicon set from `adex360_favicon.jpeg` via `src/app/icon.jpeg`. Site title set to "Adex360: E-Commerce Digital Marketing Agency". Header/nav hover + footer hovers standardized on `#E38A19`.
- **2026-08-19:** Contact Us page built (form UI + info column). SEO Services page built from WP screenshots (12 sections, own metadata, static prerender).
- **2026-08-19:** SEO Services refinement pass: services block rebuilt as interactive 6-tab component (WP per-tab content, shape-morph blob, booking-widget CTAs); Unique stats rebuilt to match the home stats showcase (45% donut + NEW TRAFFIC dark card); Growth section rebuilt as animated dashboard (self-drawing gradient chart, floating client chips); Score CTA band redesigned (aurora glows, masked dot grid, score chip, split inputs); testimonials rebuilt as the home Swiper carousel with the 4 real WP quotes (names corrected: Faulkner W., Jade P.); FAQ answers replaced with exact WP content; projects banner slug changed to `/portfolio`. Carousel arrow borders fixed site-wide (`border-black/10` → visible `#B6BEDB`).
- **2026-08-19:** Branded under-construction 404 page added (`not-found.tsx` + `UnderConstruction.tsx`) — all unbuilt routes now show an animated "under construction" screen with proper 404 status.
- **2026-08-19:** Social Media Management page built (12 sections, static prerender). Adversarial multi-agent review ran over the new page; 10 confirmed findings fixed across the codebase: chip float loops switched to yPercent so they can't fight scroll-gated entrance tweens (Social/SeoGrowth — real visual bug — plus Social/SeoHero hardening); FAQ accordions got h3 question headings + aria-hidden collapsed panels + aria-controls (Social+Seo); testimonial star rows got sr-only rating text and carousels now stop autoplay under prefers-reduced-motion and on keyboard focus (all 3 + BrandsMarquee); decorative hero/dashboard visuals aria-hidden (Social/Seo heroes + growth, ScoreCta chips); service-tab buttons use aria-current + aria-controls instead of aria-pressed (all 3 pages); contrast fixes ("Consistent growth" pill text → #995607 in 3 stats showcases; social hero gradient darkened).
- **2026-08-19:** Site-wide GSAP pass. Migration plan §3 updated: GSAP + ScrollTrigger is the official animation stack (Framer Motion dropped). Gaps filled: contact-form field stagger + success spring, FAQ item stagger, SeoHero entrance timeline, SeoAdvantage list cascades, footer column reveals. New `data-parallax` scrub attribute in ScrollFx, applied across all 3 pages (glows, illustrations, card grids, comparison cards at different depths). All scroll reveals + bespoke timelines made reversible: play on scroll-down, reverse on scroll-up, replay on re-entry (counters stay one-shot).
- **2026-08-20:** Contact Us form replaced with the real GHL (GoHighLevel/LeadConnector) iframe embed the user provided — submissions now go straight to their GHL account. Removed the static React-Hook-Form UI in favor of it; Phase 5's "contact form API route" item is no longer needed for this page.
- **2026-08-20:** SEO hero blob upgraded from radius-yoyo to 3s random shape cycling (blob/arch/diamond/parallelogram/egg/leaf/squircle, elastic morph + icon counter-transform). Shape makers extracted to shared `src/lib/blobShapes.ts`; SeoStrategies/SeoLocal/SocialTabs deduplicated to use it. Saved as the standard for all future service-page heroes (memory + Engineering Notes).
- **2026-08-20:** Performance Marketing page built (`/performance-marketing`, 11 sections, static prerender). All 5 tab CTAs point to `/contact-us` per explicit user instruction (breaking from the SEO/Social pattern of a booking-widget link on the last tab). FAQ intentionally preserves the WP source's duplicate-question quirk (Q5/Q14 identical question, different answers). 3 content blocks (Unique paragraph+CTA, Growth eyebrow+paragraph, Advantage's 8 list items) were reconstructed from a lower-resolution screenshot and flagged to the user for a confirmation pass.
- **2026-08-20:** GHL contact-form top-padding investigated and fixed. Confirmed via fetching GHL's live stylesheet that the requested selectors can't be styled from our CSS (cross-origin iframe — same-origin policy blocks it regardless of `!important`); measured the real source (`#_builder-form{margin:20px}` + `.fields-container{padding:30px 0!important}` = 50px) and implemented a self-adjusting visual crop (`overflow-hidden` + negative `marginTop` on the iframe) instead. Documented that the only place the requested CSS rule will actually apply is the form's Custom CSS panel inside the GHL dashboard.
- **2026-08-20:** Mobile menu rebuilt from a basic top-accordion panel into a premium right-to-left slide-in drawer (`Header.tsx`), based on a reference screenshot: search bar, gradient icon-tile rows for every nav item, expandable categories with per-child icons and an inline gradient caption card, "Start a Project" CTA + phone/email chips in the footer, hover/focus micro-interactions throughout. Fixed a real layout bug along the way: the drawer and its backdrop, nested inside `<header>`, were rendering `position: fixed` relative to the header's own ~80px box instead of the full viewport — because `backdrop-blur` (a `backdrop-filter`) makes an element a CSS containing block for its `fixed` descendants — so only the top strip painted white and the rest of the drawer showed the page bleeding through underneath it. Fixed by moving the drawer/backdrop to be siblings of `<header>` instead of children.
- **2026-08-20:** Removed all placeholder/fake content the user flagged across the site rather than leaving it live: the Hero's custom "Reviewed On Clutch" badge, the "As Featured On" `ClutchStrip` (removed from Home, Contact Us, SEO Services, Social Media Management, and Performance Marketing — component deleted), and the Home page's "Our Latest Media" placeholder blog section (`Blog.tsx`, deleted). All three come back once real backing content exists (a Clutch embed code, and the Phase 4 blog backend, respectively).
- **2026-08-20:** Contact Us page's info column redesigned to match a reference layout: "Talk To Us" (2 hotline cards, UK + PK) → Email card → "Our Offices" (divider header, all 3 offices — UK/US/Pakistan — instead of just UK). Replaced flag emoji (which render as raw "GB"/"US"/"PK" text on Windows, no emoji font fallback) with hand-built inline SVG flags for reliable cross-platform rendering. Fixed phone numbers getting cut off with an ellipsis by moving the 2-up card breakpoint from `xs` (480px) to `sm` (640px) and swapping `truncate` for `break-words`.
- **2026-08-20:** Discussed email delivery for the SEO-score form's "Free SEO Audit" submissions (destination: `munib.ahmad@adex360.com`). No email service was previously wired up (form only toggled a local "sent" UI state). Presented three real implementation paths — Resend, SMTP via the existing mailbox, or a GoHighLevel webhook — with tradeoffs; awaiting the user's choice before writing the API route.
- **2026-08-20:** Added `docs/task-log.md` — a running, dated, per-task companion to this file (raw request-by-request history vs. this file's phase/feature status view), plus a standing memory (`task-log-file`) so it gets appended to after every completed task going forward without being asked each time.
- **2026-08-20:** Contact Us info column follow-up fixes: UK/PK hotline cards + Email stacked into one column (was 2-up on `sm:`); "Our Offices" divider heading left-aligned on desktop.
- **2026-08-20:** Fixed a Home page project-card name typo ("Wellew Home" → "Weltew Home") in `Projects.tsx`. Created `public/images/projects/` for real project screenshots; once the user uploaded 4 (Butterfly, Nishat UAE, Logo Official, Weltew Home — renamed to kebab-case for URL safety), wired them into the Home page's Projects grid via `next/image` (`object-cover object-top`), removing the solid-gradient-with-name-text placeholder. SEO/Social/Performance project grids still used the placeholder pattern at the time — since resolved, see 2026-08-21.
- **2026-08-20:** Web Development page built (`/web-development`, 10 sections, static prerender) — 4th service page. Kept the site's established dark-gradient hero-blob-cycling design rather than the WP source's literal light hero, for consistency with the 3 other already-shipped service pages. "Why Our Code Wins" section uses a teal-accented icon composition in place of a raster illustration (none was provided for that section). All 6 tab CTAs default to `/contact-us` (no explicit CTA targets were given for this page). Testimonials deduped from the WP swiper's looped/duplicated slide markup down to 4 unique quotes; FAQ carries all 13 WP Q&As.
- **2026-08-20:** Shopify App Development page built (`/shopify-app-development`, 10 sections, static prerender) — 5th service page. "Apps We Developed" project cards link to real (not-yet-built) slugs the user specified — `/universal-product-feed`, `/pushbot`, `/mailbot` — rather than the usual placeholder pattern; they resolve to the branded under-construction page until built. Testimonials had no role/title field in the source this time, so the card layout was simplified to name + quote only. Growth section reused the Web Development page's icon-composition pattern in a blue/violet accent instead of teal. FAQ carries all 15 WP Q&As ("Knowledge Kit").
- **2026-08-20:** Real app icons wired into the Shopify page's "Apps We Developed" cards. User dropped 3 images into `public/images/apps/` (renamed to `universal-product-feed.png`, `pushbot.png`, `mailbot.png`); Universal Product Feed/PushBot render `object-contain` over their existing colored gradient tile (icon-on-white exports), Mailbot renders `object-cover` with no extra tile since its export already has a full-bleed purple background.
- **2026-08-20:** Added the 3 individual Shopify app pages (`/universal-product-feed`, `/pushbot`, `/mailbot`) to `docs/migration-plan.md` §4 Site Architecture, flagged as net-new pages outside the original WordPress sitemap (the client gave their slugs when building the Shopify page's project cards, but hadn't been formally recorded in the plan yet). Also added a matching `[ ]` checklist line in this file's Phase 3 section.
- **2026-08-20:** CRM Integration page built (`/crm-integration`, 9 sections, static prerender) — **the 6th and last originally-planned service page**, completing `migration-plan.md` §4's full service-page list. Notable deviations from the established pattern, confirmed against the source screenshots rather than assumed: 5 tabs instead of 6, no percentage-arc hero stat chip, and no "Previous Projects" section at all. Testimonials reused the exact same 4-quote WP swiper content as the Web Development page (identical source markup). FAQ carries all 11 WP Q&As.
- **2026-08-20:** Universal Product Feed page built (`/universal-product-feed`, static prerender) — 1st of the 3 net-new individual Shopify app pages. Unlike every prior service page, the source is a plain WP article (Elementor text-editor content, no hero stats/tabs/testimonials/FAQ), so a new reusable template was built instead of the usual per-page component set: `AppCaseStudy.tsx` (dark hero + bulleted Goals/Features/Results sections + Challenges-and-Solutions card list + closing CTA band) driven by a typed content object, with per-app content split into its own file so PushBot and Mailbot can reuse the same component. Icons are passed as string keys rather than lucide components to keep the Server-Component page → Client-Component template boundary serializable.
- **2026-08-20:** Pushbot page built (`/pushbot`, static prerender) — 2nd of the 3 individual Shopify app pages, reusing the `AppCaseStudy` template with its own content file (`pushbot.content.ts`) and a new `Bell` icon key added to the template's icon map.
- **2026-08-20:** Mailbot page built (`/mailbot`, static prerender) — 3rd and last individual Shopify app page, reusing the `AppCaseStudy` template with its own content file (`mailbot.content.ts`) and the existing `Mail` icon key (pink-to-violet gradient). All 3 "Apps We Developed" cards on `/shopify-app-development` now resolve to real, fully built pages instead of the under-construction fallback.
- **2026-08-20:** About Us page built (`/about-us`, 7 sections, static prerender) — first of the 2 remaining company pages. Reused `Team` and `BrandsMarquee` from the Home page outright (identical source content: same 4 team members, same "Brands Impacted" heading and a brand set already covered by the existing 16-logo carousel). 5 new components built for the page's unique sections (hero, "What We Do", "Perfect Fit", testimonials, projects); testimonials reused the same 4 WP quotes as Web Development/CRM. The source page's real office photo had no uploaded asset, so the hero panel uses an icon composition instead (consistent with how other pages handle missing raster assets). The Clutch review-widget row at the bottom of the source was deliberately left out per explicit user instruction.
- **2026-08-20:** FAQ's page built (`/faqs`, static prerender) — second and last company page, completing Phase 3's page list apart from Portfolio. Correctly identified and skipped the source's `elementor-hidden-*` legacy accordion (leftover theme-demo Lorem-ipsum content, permanently hidden) in favor of the real 10-question `n-accordion`. FAQ answers carry real internal links the source pointed at existing site routes. The "Still need help?" CTA band's filler paragraph (WP theme-demo boilerplate, not real brand copy) was rewritten rather than carried over.
- **2026-08-20:** Header "Company" dropdown and footer "Case Studies" link both repointed from `/case-studies` to `/portfolio`, per explicit user confirmation that Portfolio replaces the never-built Case Studies page outright rather than existing alongside it. No `/case-studies` references remain anywhere in the codebase.
- **2026-08-21:** Portfolio page built (`/portfolio`, static prerender) — the last page in Phase 3's originally-scoped list. 16 project cards, each linking to its real WP slug; 3 already resolve to real pages built earlier this phase (Universal Product Feed/Mailbot/Pushbot), the other 13 are net-new individual case-study pages not yet built (not yet scoped by the user). Added a minimal page heading not present in the WP source, for on-page SEO/UX consistency with every other interior page. Clutch achievement badges excluded per explicit user instruction.
- **2026-08-21:** Real project images wired into all 16 Portfolio cards, fetched directly from the source HTML's `adex360.com/wp-content/uploads/...` URLs via `curl` (14 unique files, 2 reused twice each per the source markup) — no placeholders remain on the page.
- **2026-08-21:** User flagged that `SeoProjects`, `SocialProjects`, `PerformanceProjects`, and `WebDevProjects` — the "Featured Projects" sections on 4 service pages — still used solid-gradient-with-name-text placeholders. All 12 project names on those 4 sections turned out to be exact matches for entries already in the Portfolio grid, so no new content/images were needed from the user — reused the same `public/images/portfolio/*` files already fetched, and added a real case-study-slug `Link` to each card (matching the Portfolio grid's treatment) instead of leaving them as static unlinked tiles. Also strengthened the standing `task-log-file` memory: the user explicitly reaffirmed that both `progress.md` and `task-log.md` must be updated after every task, unprompted, including small fixes — not just new pages.
- **2026-08-21:** Added the Portfolio grid's hover-arrow treatment (gradient scrim over the image + a circular `ArrowUpRight` badge that fades in on hover, bottom-right of the card) to all 4 service pages' project cards (`SeoProjects`, `SocialProjects`, `PerformanceProjects`, `WebDevProjects`), matching `PortfolioGrid.tsx` exactly — now every project card site-wide that links somewhere shows the same "this is clickable" affordance.
- **2026-08-21:** Gave the arrow badge its own micro-interaction, independent of the card-level hover: it sits diagonal (↗) by default, and rotates to horizontal (→) only while the pointer is directly over the badge itself, reverting to diagonal the instant the pointer leaves it — even if the card is still hovered. Implemented with a nested Tailwind v4 named group (`group/arrow` on the badge, `group-hover/arrow:rotate-[-45deg]` on the icon) so it's scoped separately from the outer card's `group` (which still drives the image zoom + badge fade-in). Applied identically across `PortfolioGrid.tsx` and all 4 service pages' project cards; confirmed the compiled CSS rule exists in the production build output before calling it done.
- **2026-08-21:** Flipped the arrow's hover rotation from `-45deg` to `45deg` per user request (same 5 files as above), verified the rendered markup only carries the `45deg` class now (a stale `-45deg` rule lingered in a Tailwind/Turbopack cache chunk even after a clean `.next` rebuild — harmless dead CSS since no element references it, confirmed by checking the actual rendered class list rather than trusting the CSS chunk alone).
- **2026-08-21:** BeOneShopOne case-study page built (`/beoneshopone`, static prerender) — 1st of the 13 individual case-study pages linked from Portfolio. A different content shape than every prior page (narrative SEO results case study with nested subsections and inline stat callouts, not a service/app page), so a lighter reusable shell was built instead of a rigid data-driven template: `CaseStudyHero`, `CaseStudyCta`, and a small `Stat` component, with the actual article body hand-authored per page since case-study structures vary too much to force into one schema. Hero illustration fetched directly from the user's local XAMPP server via `curl` (confirmed this works the same way the earlier `adex360.com` production fetches did, since Bash runs on the user's own machine). Skipped the source's permanently-hidden "Project Info" sidebar widget, same pattern as the FAQ page's hidden legacy accordion.
- **2026-08-21:** EU Naturals case-study page built (`/eu`, static prerender) — 2nd of the 13 individual case-study pages, and the introduction of a second reusable "brand story" template (`src/components/casestudy/brand/*`: hero with cross-links to sibling case studies, stats band, challenge/solution/result trio, services card, key-teams band, related-case-studies grid) alongside the narrative template used for BeOneShopOne. `CaseStudyCta` was generalized with props instead of duplicated so both templates share it. All 3 real images fetched from the user's local XAMPP server via `curl`. Corrected a leftover unrelated brand name ("AAFMAA") in the source's Seona-card quote to match the actual page being linked.
- **2026-08-21:** AK Galleria case-study page built (`/ak-galleria`, static prerender) — 3rd of the 13 individual case-study pages. This one's content shape matched the existing `AppCaseStudy` template exactly, so no 3rd template was built — reused it directly with a new `ShoppingBag` icon and an opt-in `showBackToPortfolio` prop (the 3 existing Shopify app pages don't show it, keeping them unchanged). Fixed a stray `<br>` in the source that had split one bullet's text across two separate list items.
- **2026-08-21:** Butterfly Sanitary Napkin case-study page built (`/butterfly`, static prerender) — 4th of the 13 individual case-study pages, built from HTML only (no screenshots this time). Reused the BeOneShopOne narrative template, plus a new shared `NumberedList` component for this page's "bold title, paragraph below" numbered sections — a pattern not covered by BeOneShopOne's flat bullet lists.
- **2026-08-21:** Standardized the "Back to Portfolio" link into one reusable `BackToPortfolio.tsx` pill button (icon + label, light/dark variants, hover slide) and wired it into all 3 case-study templates — previously it was a plain text link inline-styled per template (present on 2 of them, missing from EU's `BrandHero`), and the app-page template (`AppCaseStudy`) only showed it conditionally. Now every case-study-family page shows it unconditionally, including the 3 Shopify app pages (which are also linked from the Portfolio grid, so this was a real gap, not just a cosmetic one) — 7 pages total.
- **2026-08-21:** Logo Official case-study page built (`/logo-official`, static prerender) — 5th of the 13 individual case-study pages. Another exact-match reuse of `AppCaseStudy` (same shape as AK Galleria), with a new `Footprints` icon; no template changes needed.
- **2026-08-21:** Weltew Home case-study page built (`/weltew-home`, static prerender) — 6th of the 13 individual case-study pages, same `AppCaseStudy` shape with a new `Sofa` icon. Generalized `AppCaseStudyContent.overview` from `string` to `ReactNode` to support this page's inline orange-highlighted stats, backward-compatible with all prior pages.
- **2026-08-21:** ECS case-study page built (`/ecs`, static prerender) — 7th of the 13 individual case-study pages, narrative template. New shared `LabeledBullets` component for a "bold label: description" bullet pattern recurring 3 times on this page.
- **2026-08-21:** Nishat USA case-study page built (`/nishat-usa`, static prerender) — 8th of the 13 individual case-study pages, narrative template, near-identical shape to ECS. Extracted ECS's `ChallengeBlock` helper into a shared component once this page confirmed the problem/solution card pattern recurs.
- **2026-08-21:** Nishat UAE case-study page built (`/nishat-uae`, static prerender) — 9th of the 13 individual case-study pages, narrative template with the heaviest stat density yet (15+ inline highlights). Its "Challenges & Solutions" section only gives a Solution (no Problem statement), so used a lighter page-local `SolutionBlock` instead of the shared `ChallengeBlock`.
- **2026-08-21:** ONE case-study page built (`/one`, static prerender) — 10th of the 13 individual case-study pages, narrative template, a clean reuse of `ChallengeBlock` and `LabeledBullets` with no new components needed.
- **2026-08-25:** Old WordPress `/tag/*` archive URLs (still indexed by Google, discovered via a live search for "adex360") redirected to the closest real page via keyword-matched `redirects()` rules in `next.config.ts`. Done directly on `main`, not `blog-posts` — the site is live, so this shipped without needing anything from the blog branch. See Phase 6 for the full writeup.
- **2026-08-25:** Fixed Team/footer sections getting stuck invisible when the home Projects filter shrinks the page (`ScrollFx.tsx` now watches `document.body` for resizes and re-runs `ScrollTrigger.refresh()`). Committed to `main` alongside the redirect fix, then carried onto `blog-posts` unchanged so both branches share the identical fix.
- **2026-08-25/27:** Admin dashboard rebuilt around two tabs, Blog Posts and Projects — new `Project`/`Industry` Prisma models, the home page's "Latest Projects" section made fully DB-driven and automatic (always the 4 most recent published projects, no manual toggle), all 16 real `/portfolio` projects seeded in. Iterated down from an initial richer form (case-study link, sort order, slug, summary, "featured" checkbox) to a minimal one (name/industry/service/status/image) based on direct feedback that the cards are display-only. Full writeup in the Phase 4 "Projects dashboard tab" entry above. Done entirely on `blog-posts`.
- **2026-08-27:** Home blog slider's arrows/loop fixed to only activate when a breakpoint actually has more posts than it can show at once (`watchOverflow` + a mirrored `isLocked` React state), replacing the earlier flat `posts.length > 1` check that showed a pointless slider on desktop with only 2 posts. Added top spacing to the `/portfolio` and `/resources` grid sections, and widened `/resources`' post grid from 2 to 3 columns on desktop.
- **2026-08-27:** Deleted the 6 `[TEST]`/`[TEST DRAFT]` blog posts seeded 2026-08-25, removed the unused "Adex360" category and "Munib Ahmad" author, and renamed the default author from "Adex360 Admin" to "Adex360" (`.env` updated to match for future reseeds). Surfaced but did not resolve: the one genuine blog post ("Landing Pages Paid Ads and Funnels…") went missing between the test-post cleanup and the taxonomy cleanup, likely deleted via the dashboard UI outside this session — flagged to the user.
- **2026-08-27:** All of the above (redirects/ScrollFx fix aside, which were already on `main`) committed and pushed to `blog-posts` as a single commit (`6bf522e`, 29 files). `main` remains blog-free; the two branches are documented at the top of this file under "Branch structure."
