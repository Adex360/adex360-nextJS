/** How many of the most recently published projects the home page's "Latest
 *  Projects" section shows. Shared by the home page query and the admin
 *  dashboard's "on home page" indicator so the two can never disagree. */
export const HOME_PROJECTS_COUNT = 4;
